# 省脚力路线卡｜优化报告

本轮优化围绕"攻略只说哪里好玩，不告诉一路走多久、哪里能坐下休息、找厕所是否方便"这一原始痛点，补齐了三类落差：①把"告诉"缺的一半补上（附近厕所/歇脚点站内检索与逐属性确认写入、开放时间从地图文本到等待开门/闭门冲突计算的全链路可达、高德标注阶梯进入路段行提示）；②让整理成果不再易失（行程 JSON 导出/导入备份）；③降低录入摩擦并清理技术债（批量采纳地图估算、rest-candidate 编辑入口补齐、planner 过期注释修正、导出装箱逻辑统一），并在回归中修复了"最后一站→终点"路段行缺失缺陷（Task 11），使每个相邻节点之间的路段都可编辑、可记录阶梯核对。全程保持"未知不等于没有"与来源可追溯的诚实性红线，未引入账号、云端、大模型等超范围能力。验证结果：`npm test` 16 个文件 **204 通过 / 5 skipped**（5 个 skipped 为 amap-live 集成用例，默认跳过属正常），`npm run test:e2e` **22 passed**（desktop 11＋mobile 11，含 1 条 perf 测量用例），`npm run build` 通过；性能基线本轮首次建立，编辑交互中位 **16.7ms / 17.4ms**（desktop / Pixel 7 仿真），远低于需求 9.3 的 300ms 预算。（第二轮增量与最新回归实测见文末"七、第二轮增量"。）

---

## 一、原始需求逐条对照

对照《省脚力路线卡-需求说明书》与原始诉求（选景点成路线卡、每段预计步行时间、休息点/厕所/台阶提醒、"累了就跳过"、大字版导出图片）：

| # | 原始诉求 | 对应需求条目 | 优化前状态 | 优化后状态 | 证据/测试 |
| --- | --- | --- | --- | --- | --- |
| 1 | 选景点成路线卡：把选好的景点整理成可调整的路线 | R01 行程创建与保存、R02 景点与路线编辑 | 已完整（手动编辑、排序、自动保存、本地持久化） | 保持完整；新增 JSON 备份导出/导入，整理成果可携带 | `tests/app/App.test.tsx`（12 用例）、`tests/storage/local.test.ts`（14 用例）、`tests/e2e/journey.spec.ts` 备份 roundtrip 用例 |
| 2 | 每段预计步行时间：知道一路走多久 | R02 路段记录、R04 时间与步行负担汇总 | 记录/汇总口径严谨，但存在缺陷："最后一站→终点"路段行未渲染，该段无法编辑、阶梯提示与台阶核对不可达 | 每个相邻节点之间的路段（含到终点）均有独立路段行，可编辑"其中步行/接驳全程"、可记录台阶核对；汇总口径未改动 | `tests/features/planning.test.tsx`「终点前路段行可编辑并显示阶梯提示（Task 11）」（本轮新增，已验证在缺陷存在时失败）、`tests/domain/compute.test.ts`（统一样例 43/100/28 分钟不变）、`tests/domain/itinerary.test.ts` |
| 3 | 休息点提醒：知道哪里能坐下休息 | R03 设施与提醒 | 歇脚信息只能用户凭记忆逐字段手工录入；周边歇脚处检索后端就绪但无任何 UI 入口；rest-candidate 无编辑入口 | 站内检索附近歇脚处候选（名称/地址/直线距离＋"直线距离，非步行路程"标注），逐属性确认"是否可坐"三态（默认待确认，绝不默认可坐/不可坐）后写入；卡片提示措辞为"歇脚点候选"，不暗示空位 | `tests/features/facilityDialog.test.tsx`（7 用例）、`tests/app/App.test.tsx`「附近设施候选流程」（2 用例）、`tests/features/buildViewModel.test.ts`、e2e「附近候选逐属性确认写入」用例 |
| 4 | 厕所/台阶提醒：知道找厕所是否方便、有无阶梯 | R03 设施与提醒 | 厕所周边检索同样无 UI 入口，信息获取成本全压回用户；地图标注台阶（`walk_type=20`）被前端丢弃 | 站内检索附近厕所候选并逐属性确认开放情况后写入；路段行显示"高德标注本段可能有阶梯（待核对）"＋"记录台阶核对"一键核对，核对事实保留；未返回阶梯绝不显示"无台阶" | 同上＋`tests/features/planning.test.tsx`「地图报告阶梯…（R-A3）」、e2e「阶梯提示展示与一键核对后事实保留」用例 |
| 5 | "累了就跳过"：给爸妈体面的退出机制 | R07 途中跳站 | 已完整（跳过保留资料可恢复、必去景点二次确认、受影响路段失效） | 保持完整，无回退 | `tests/app/App.test.tsx`「跳过可选景点…（R07）」、`tests/e2e/journey.spec.ts` 跳站用例 |
| 6 | 大字版导出图片发家庭群 | R05 大字路线卡、R06 图片导出 | 已完整（多页连续卡片、页序、失败可重试）；但导出装箱逻辑在 `paginate.ts` 与 `measure.ts` 重复（技术债） | 保持完整；装箱统一为 `packBlocks` 唯一实现，分页行为不变并固化回归用例 | `tests/features/paginate.test.ts`（7 用例，含 6 景点＋超长备注分页场景）、e2e 导出用例与 perf 测量 |

补充说明：开放时间虽属第 3/4 项"设施提醒"范畴，但其价值在"等待开门计入总用时、闭门冲突提醒"（R04 口径），本轮已打通该链路（见下表缺口 2）。

## 二、优化前后对比数据

### 2.1 功能缺口（优化前 → 优化后）

| # | 功能缺口 | 优化前 | 优化后 | 验证 |
| --- | --- | --- | --- | --- |
| 1 | 周边厕所/歇脚点检索 | 后端完整（`server/api-core.ts` nearby），前端 `searchNearby` 无任何 UI 调用（README 自认"界面入口未接入"） | FacilityDialog"搜索附近"（厕所/歇脚处、半径 300/500/1000/2000m），候选＋逐属性确认写入，含来源/POI id/获取时间/核对日期 | facilityDialog 7＋App 2＋e2e 1 |
| 2 | 开放时间计算链路 | 模型＋等待开门/闭门冲突计算齐备，但无任何录入/抓取入口（`placeFromSearch` 恒 `unknownFact`），链路不可达 | 地图开放时间文本→"地图参考（待核对）"→"写入并标记核对"/结构化时段录入→等待开门计入总用时、闭门冲突提醒 | `opening.test.ts` 30＋`opening-integration.test.ts` 3＋`buildViewModel.test.ts` 14＋e2e 开放时间断言 |
| 3 | 地图阶梯数据 | 服务端已产出 `reportedFeatures`，`usePlanning` 丢弃 | 路段行提示＋一键核对；地图报告值不落盘（重载回待确认），用户核对走 stairs 事实持久化 | planning 2＋itinerary/local 各有覆盖＋e2e 1 |
| 4 | rest-candidate 编辑 | FacilityDialog 仅 toilet/stairs，歇脚处无编辑入口 | 补齐名称/位置/是否可坐三态编辑，"没查到"不记"不可坐" | facilityDialog 7 |
| 5 | 数据易失 | 单份 localStorage，无导入导出，清缓存/换设备即丢失 | 导出/导入 JSON 备份（走 `toPersisted` 白名单，未采纳地图值不导出；导入失败持久报错且当前行程不变，成功需二次确认） | `local.test.ts` 14＋App 3＋e2e roundtrip 1 |
| 6 | 地图估算采纳摩擦 | 需逐段采纳 | "采纳全部地图估算（N 段）"批量采纳，仅 `durationSource=amap` 段被采纳，manual/adopted/same-entrance 不覆盖；"采纳≠实地核实"说明保留 | App 1＋itinerary 1 |
| 7 | planner 过期注释 | 注释所称 UTC 日秒偏差与实际行为（`epochToLocalDaySeconds` 本地日秒）矛盾 | 核实实际行为后修正注释，计算逻辑未动 | `planner.test.ts` 21＋`compute.test.ts` 11 无回归 |
| 8 | 导出装箱逻辑重复 | `packBlocks` 与 `measure.ts` 内联装箱重复，且生产管线不用 `packBlocks` | `packBlocks` 成为唯一装箱实现（PackOps 注入 measure/split），`paginateCard` 复用，T15/D13 分页场景固化为用例 | `paginate.test.ts` 7 |
| 9 | 终点前路段行缺失（回归新发现，违反 R02/R-A3 覆盖） | 终点分支提前 return，"最后一站→终点"的 LegRow 从未渲染，该段无法编辑/记录台阶 | 终点分支同样渲染 LegRow（起点为 seq[0] 无前序路段，不受影响） | 本轮新增 `planning.test.tsx` 用例（已反向验证：去掉修复该用例即失败）；e2e 阶梯用例同步改为断言两段均有提示 |

### 2.2 测试数量

| 口径 | 优化前（评审时基线） | 优化后（本轮最终回归实测） | 变化 |
| --- | --- | --- | --- |
| vitest 单元/组件 | 约 128 项 | **209 项：204 通过 / 5 skipped**（16 个文件；5 个 skipped 为 amap-live 集成用例，默认跳过属正常） | 约 +81 项 |
| Playwright e2e | 7 用例 ×2 project ＝ **14 次运行** | 11 用例 ×2 project ＝ **22 次运行**（desktop 11＋mobile 11，含 1 条 perf 测量用例），全部通过 | ＋8 次运行 |
| 本轮（Task 11）增量 | — | ＋1 条 vitest 用例（`tests/features/planning.test.tsx`：终点前路段行可编辑＋阶梯提示），203→204 通过 | ＋1 |

主要分布（实测）：server/amap 42、server/api 22、workers/planner 21、storage/local 14、features/buildViewModel 14、domain/opening 30、domain/compute 11、app/App 12、features/facilityDialog 7、domain/itinerary 7、features/paginate 7、domain/status 5、domain/planning 4、features/planning 4、domain/opening-integration 3、integration/amap-live 6（其中 5 skipped）。

### 2.3 性能基线（本轮首次建立）

优化前无任何性能基线数据。本轮以可复跑的 `tests/e2e/perf.spec.ts` 首次建立基线；因首测即达标（≪300ms），**未改任何 `src` 性能代码**，故无"性能修复前/后"差异，下表列首测基线与本轮回归复测（口径一致，可直接对比）：

| 指标 | 首测基线（建立时） | 本轮回归复测 | 需求预算 | 结论 |
| --- | --- | --- | --- | --- |
| 编辑交互响应（"确认修改"→纸面可见）中位 | 16.5ms（desktop 与 Pixel 7 一致，样本 15.2–21.0ms） | desktop **16.7ms**、mobile **17.4ms**（样本 16.4–25.0ms） | ≤300ms（需求 9.3） | 达标（约为预算的 1/17），复测无退化 |
| 导出耗时（点导出→"图片已生成"）中位 | desktop 1557ms、mobile 1594ms | desktop **1535.8ms**、mobile **1552.5ms** | 无验收预算 | 约 1.5 秒量级，复测无退化 |
| 并行负载复跑 | 编辑交互约 18ms | — | — | 无退化 |

场景口径：6 景点典型行程＋长备注（约 3 页导出），视口 1280×900；每次测量编辑 5 样本、导出 3 样本，取中位数/均值。

## 三、关键改进点

1. **设施信息从"你去查"变"我帮你查"**：安排者不再需要翻攻略、翻地图找厕所和歇脚处——站内按类别/半径检索附近候选，逐属性确认后写入本站设施记录（来源、POI id、获取时间、核对日期全保留）；候选措辞克制："直线距离，非步行路程"紧邻标注、"歇脚点候选"不暗示有空座位或可免费休息。
2. **开放时间与阶梯两条"死链路"打通**：开放时间从地图文本（"地图参考（待核对）"）→用户核对/保守解析（首轮仅全周统一 `HH:mm-HH:mm`，第二轮扩展至分日/多时段严格文法，解析不了的仍不编造）→结构化时段，最终使等待开门计入总用时、闭门冲突提醒真正可达；高德标注阶梯从被丢弃变为路段行"可能有阶梯（待核对）"＋一键核对，且未返回阶梯绝不显示"无台阶"。
3. **数据可携带**：JSON 备份导出/导入解决"清缓存/换设备即丢失"的低频工具致命摩擦；备份与本机持久化走同一白名单（未采纳地图值不随备份走），导入失败持久报错且当前行程不变。
4. **录入摩擦下降与技术债清理**：地图估算一键批量采纳（仅 amap 段、采纳≠实地核实、来源与时间保留）；候选确认替代逐字段手工录入；`planner.ts` 过期注释修正、导出装箱统一为 `packBlocks` 唯一实现，分页行为固化为回归用例。
5. **诚实性红线全部保持，并修复覆盖缺口**：来源/核对状态逐项记录（地图参考待核对 / 自动解析请核对 / 你已核对＋核对日期 / 待确认四态）、"未知不等于没有"贯穿新增 UI（三态默认待确认）、地图报告值不落盘（重载回待确认）；全程未出现"放心走""绝对不累"类超出数据能力的表述。回归中新发现并修复"最后一站→终点"路段行缺失缺陷（R02/R-A3 覆盖补全），新增用例已反向验证其能拦住该缺陷。

## 四、遗留人工验证项

以下事项**未**由自动化验证覆盖，需人工核实，不得视为已验证：

- 微信内嵌浏览器、iOS Safari 中"保存图片到相册"需真机人工验证（自动化仅验证到"图片已生成"与下载链路）。
- 读屏（屏幕阅读器）实际朗读体验与 200% 缩放实测。
- 高德 Web 服务配额 / QPS 上限待高德控制台确认。
- 地图数据展示、署名及导出许可待核实（如后续使用真实地图截图尤其需要）。

## 五、后续可优化方向

首轮遗留方向现状（前六项已在第二轮完成，逐项注明落地形态；最后一项移出范围）：

- **多行程管理**：已完成（2026-09-23，规格 implement-followup-optimizations）——本机行程索引＋分行程存储键，支持新建/切换/复制/重命名/删除整份行程，旧单份行程数据首次打开自动迁移。
- **rest-candidate 一键转正式休息点节点**：已完成（2026-09-23，规格 implement-followup-optimizations）——设施摘要候选条目"转为休息点"，在该站点后插入休息点，座位事实整份迁移（来源/核对字段保留）、候选坐标随 `location` 事实带出、原候选记录移除，受影响路段重建回"待补充"。
- **开放时间自然语言解析增强**：已完成（2026-09-23，规格 implement-followup-optimizations）——保守解析扩展到分日（严格日期组词表）/多时段文法；跨零点、24:00、词表外文本等仍整体不解析只留原文（宁缺勿假）。
- **绕路步行纳入计算（R03 未实现项）**：已完成（2026-09-23，规格 implement-followup-optimizations）——候选转休息点后绕行两段作为真实路段进入总步行/连续步行汇总（不重复累加）；休息点行另给"歇脚绕行：比直达多走约 X 分钟"的会话内直达对照，失败/缺坐标显示"绕行对比待补充"，不用直线距离估算。
- **候选事实与手动区的覆盖语义**：已完成（2026-09-23，规格 implement-followup-optimizations）——定夺为覆盖保护："确认修改"只写本次实际改动，同值同状态不重写（原来源不被抹掉）；"已核对→待确认"属显式回退，须点名属性二次确认后才执行。
- **测试 `act()` 警告收敛**：已完成（2026-09-23，规格 implement-followup-optimizations）——组件测试等待交互后的状态更新落定，回归输出不再出现 `act()` 警告。
- **跨设备同步 / 分享链接**：移出范围（需独立规格：账号/云端/隐私评估）。

## 六、数据口径与说明

- **报告生成日期**：2026-09-22。
- **测试口径**：`npm test`（vitest run，16 文件 204 通过 / 5 skipped；5 个 skipped 为 `tests/integration/amap-live.test.ts` 需真实高德凭据的集成用例，默认跳过属正常）；`npm run test:e2e`（Playwright，desktop 与 Pixel 7 两个 project 各 11 条，共 22 次运行，含 1 条 perf 测量用例）；`npm run build`（`tsc -b && vite build`）通过。以上为本报告定稿前最后一次全量回归实测结果。
- **性能测量方法**：页面内 `performance.now()` 计时；编辑交互＝「确认修改」点击→纸面出现对应文本进入下一渲染帧，导出＝点击导出→"图片已生成"出现；6 景点典型行程＋长备注（约 3 页导出）；视口 1280×900；中位数/均值取全部样本（编辑 5 样本、导出 3 样本）。
- **性能局限**：Pixel 7 project 为浏览器设备仿真（含视口与 UA 模拟），**非真机 CPU**；单机单次运行、样本量小，数据仅作基线对比与退化观测，不代表低端真机表现。e2e 无阈值断言，避免环境抖动造成误报。
- **数字来源**：优化前数据取自评审与前几轮实施记录（vitest 约 128 项、e2e 14 次运行、功能缺口 8 项）；优化后数据以本轮最终回归实测为准（含 Task 11 修复与新增用例）。

## 七、第二轮增量（implement-followup-optimizations）

第二轮不改动上文首轮的历史口径（第二节数字为首轮定稿时实测），在其之上补齐第五节所列方向，能力落地形态如下：

- **多行程管理（本机）**：行程索引＋分行程存储键，支持新建/切换/复制/重命名/删除整份行程（删除二次确认、对象点名）；旧单份行程数据首次打开自动迁移，无需手动处理。仍为本机保存，无账号与云端。
- **歇脚点候选一键转休息点**：设施摘要候选条目"转为休息点"，在指定站点后插入休息点；座位事实整份迁移（来源/核对字段保留）、候选记录随之移除、受影响路段重建回"待补充"（不用旧时间顶替）；候选坐标随 `location` 事实带出，坐标缺失时明确提示"位置待确认，暂不能自动获取步行数据"。
- **候选写入保留坐标（R-B 收尾）**：候选"确认写入"把候选坐标落 `location` 事实（Fact 坐标值，来源字段与 name/address 同规格：高德地图搜索＋POI id＋获取/核对时间）；候选无坐标记为未知，不猜坐标。
- **绕行步行对比**：休息点行显示"歇脚绕行：比直达多走约 X 分钟"（会话内"上一站→下一站"直达对照，注明按当前步行倍数口径）；对照失败/缺坐标显示"绕行对比待补充"并可手动重试，不用直线距离估算，对照数据仅会话内、不落盘。
- **开放时间解析增强**：保守解析在全周统一单段之外扩展到分日（严格日期组词表）/多时段；跨零点、24:00、乱序、词表外文本等仍整体不解析只留原文（宁缺勿假），解析结果仍需用户核对。
- **覆盖语义定夺（设施事实覆盖保护）**："确认修改"只写本次实际改动，同值同状态不重写（原来源不被抹掉）；"已核对→待确认"属显式回退，须点名属性二次确认，回退与其他改动同次保存整体提交或整体放弃。
- **测试 `act()` 警告收敛**：组件测试等待交互后的状态更新落定，回归输出不再出现 `act()` 警告。

第二轮全量回归实测（2026-09-24）：

| 命令 | 实测结果 |
| --- | --- |
| `npm test` | 16 文件 **274 通过 / 5 skipped**（5 个 skipped 仍为 amap-live 集成用例默认跳过；较首轮 204 通过净增 70 项） |
| `npm run test:e2e` | **22 passed**（desktop 11＋Pixel 7 11，含 1 条 perf 测量用例）；未放宽任何断言语义，e2e 零适配 |
| `npm run build` | `tsc -b && vite build` 通过 |
| `tests/e2e/perf.spec.ts` | 编辑交互中位 desktop **15.5ms** / Pixel 7 **15.3ms**（样本 13.7–21.1ms），与首轮回归（16.7/17.4ms）同量级、无退化，仍远低于需求 9.3 的 300ms 预算；导出耗时中位 desktop 1355.8ms / Pixel 7 1456.4ms（约 1.5 秒量级，无验收预算） |

测试适配说明：vitest 既有用例仅一处最小适配——候选写入次数断言由 3 次改 4 次（新增 `location` 事实一键一 Fact），断言语义未放宽；其余既有用例（含覆盖保护、回退二次确认）原样通过。
