# 省脚力路线卡 / EasyWalk Route Card｜详细设计说明书

- 文档版本：V1.2 设计稿
- 编写日期：2026-09-22
- 更新日期：2026-09-22
- 本次更新：补充卡片视图模型与分页契约（第 10.2、10.3 节），确定纸面预览的窄屏缩放降级规则，新增地图来源数据的采纳与离线可见规则（第 8.5 节），把导出技术验证前置为实施前的步骤 0（第 14.2 节），修正第 13.2 节用例的跨文档引用，新增待核实事项
- 上游依据：[产品需求与 UI / UE 设计说明书](./省脚力路线卡-需求说明书.md)
- 适用终端：手机、平板、电脑浏览器
- 地图选型：高德地图 Web 服务
- 当前交付边界：仅编写文档，不生成应用代码，不初始化 Git，不暂存或提交文件，不调用用户提供的 Key
- 本文中的目录、模块、接口、配置项和提交信息均为后续实现规范，不表示对应文件、服务或提交已经存在

## 1. 产品定义与设计范围

### 1.1 中英文命名

| 项目 | 名称或规则 |
| --- | --- |
| 中文全称 | 省脚力路线卡 |
| 中文简称 | 省脚力 |
| 英文名称 | EasyWalk Route Card |
| 建议项目标识 | `easywalk-route-card` |
| 中文定位语 | 少赶一站，多歇一会 |
| 页面标题 | 省脚力路线卡 · EasyWalk Route Card |
| 页面语言 | 首轮为简体中文，英文名仅作品牌副标题，不包含整站双语切换 |
| 名称可用性 | 英文名称为设计暂定名，未完成商标、域名或应用市场重名核验 |

电脑和宽平板可以同时展示中英文名；手机分两行展示或将英文名置于辅助区，不缩小中文操作字号来容纳品牌信息；导出图片以中文行程标题为第一视觉层级，产品名称只作辅助署名。

### 1.2 功能边界

- P0：手动编辑、未知状态、时间汇总、休息与设施备注、跳站、浏览器本地保存、三端预览及图片导出。
- P1：高德地点搜索、设施候选查询、实际步行路段、约束规划及重算。
- P2：可选自然语言输入，不参与事实生成或路线可行性判断。
- 不建设账号、云端行程同步、社区、预订平台或全程实时导航。
- 不保证能获得每个景点的台阶、座椅和开放信息，也不把“路线算出来了”视为已适合所有老人。

### 1.3 设计原则

1. 原始事实、用户安排、派生统计分开保存。
2. 未知、明确为零、无效输入使用不同状态。
3. 地图服务负责道路与地点数据，程序负责计算与校验，大模型不是必要依赖。
4. 任何异步结果都绑定输入版本，不覆盖更新的行程。
5. 手机、平板、电脑功能一致，布局不同，不维护三套业务代码。
6. 数据来源、草稿和超限提示随导出保留，不能只在编辑界面可见。

## 2. 技术选型与职责边界

| 层次 | 设计选型 | 原因与限制 |
| --- | --- | --- |
| 页面 | React + TypeScript + Vite | 复用三端组件，业务与表现分离；具体依赖版本在实施时固定到锁文件 |
| 样式 | 语义 CSS 变量 + CSS Modules | 使用需求说明书的中性黑灰/绿色与白底纸面令牌，不额外叠加另一套主题 |
| 交互基础 | Radix UI 对话框等无障碍基础组件、统一线性图标 | 复用焦点管理，不引入完整后台管理框架 |
| 状态 | 单一行程状态及显式更新事件 | 首轮不引入分布式状态或复杂消息总线 |
| 本地存储 | localStorage，单份当前行程 | 文本量小，无照片上传与云端同步；存储可失败，必须显式反馈 |
| 领域计算 | TypeScript 纯计算模块；自动规划放 Web Worker | 编辑/预览共享口径，不阻塞输入；不在 Worker 中持有服务端凭据 |
| 高德适配 | Node.js + Fastify，固定上游服务 | 保护 Web 服务 Key，统一验证、错误、配额与响应归一化 |
| 输入校验 | 前后端共享 Zod 数据约束 | 客户端便于即时提示，服务端仍独立验证；不信任客户端校验结果 |
| 导出 | 独立纸面组件 + 分块排版 + html-to-image | 字号固定、逐页渲染；实际浏览器兼容性是交付门槛，须先按第 14.2 节验证可行性再进入实现 |
| 测试 | Vitest + Testing Library + Playwright | 分别验证计算、交互和浏览器闭环；不能只以单元测试代替可用性验证 |

设计不锁定未经核查的“最新版”版本号；实施时选择受支持且相互兼容的版本，记录环境与锁文件。

## 3. 总体架构与模块设计

### 3.1 数据流

```text
手机 / 平板 / 电脑
        │
        ▼
行程编辑 → 行程状态 → 本地保存
              │
              ├→ 校验与统计 → 大字卡预览 → 分页导出
              │
              └→ 规划输入快照
                    ├→ 本站高德适配 API → 高德 Web 服务
                    └→ 路段结果 + 约束 → 规划 Worker
                                          │
                                          ▼
                              候选结果与差异 → 用户确认应用
```

- 前端只调用本站固定 API，不把 Web 服务 Key 发到浏览器。
- 地图请求只发送检索或路线所需参数；用户完整备注、姓名、标题不随路径请求发送给高德。
- 计算结果先进入候选区，用户确认后才修改当前路线。
- 图片导出在本地完成，不上传行程截图。

### 3.2 建议模块清单

下列路径仅表示将来的职责分配，本轮不创建这些代码文件。

| 模块 | 建议位置 | 输入 | 输出及职责 |
| --- | --- | --- | --- |
| 应用壳与视图切换 | `src/app/` | 当前视口、编辑/预览状态 | 三端布局和导航；不参与路段计算 |
| 行程编辑 | `src/features/itinerary/` | 当前行程、编辑事件 | 新增/重排/跳过/恢复及局部草稿 |
| 设施编辑 | `src/features/facilities/` | 设施事实与核对状态 | 逐属性修改，不一键确认全部信息 |
| 规划面板 | `src/features/planning/` | 约束、API 状态、候选结果 | 输入校验、差异确认、错误处理 |
| 大字卡 | `src/features/route-card/` | 统一 CardViewModel（第 10.2 节） | 只读纸面，不读取全局编辑状态或原始地图响应 |
| 图片导出 | `src/features/export/` | 行程快照、CardViewModel | 按第 10.3 节分页、图片 Blob、保存反馈 |
| 领域模型与计算 | `src/domain/` | 归一化的节点、路段、约束 | 校验、统计、时间窗口和候选比较 |
| 规划 Worker | `src/workers/` | 有版本号的输入快照 | 有界搜索、候选、冲突、搜索完整度 |
| 本地存储 | `src/storage/` | 已提交的有效领域数据 | 保存与恢复；损坏数据不静默清空 |
| API 客户端 | `src/services/` | 受限本站请求参数 | 取消、请求关联和响应解析 |
| 服务端路由 | `server/routes/` | 已校验请求 | 调用适配层，返回统一结果 |
| 高德适配 | `server/amap/` | 固定检索/路线参数 | 附加 Key、访问上游、归一化、隐藏敏感信息 |
| 请求契约 | `shared/contracts/` | API 请求与响应 | 字段类型、枚举和边界约束 |

### 3.3 状态所有权

- 行程状态拥有用户已确认的数据；表单输入中的非法文本不进入领域模型。
- 局部面板拥有尚未确认的草稿，取消仅丢弃这一份局部草稿。
- 统计数据由领域计算派生，不作为可独立修改的另一份事实。
- 地图响应只在适配层解析，UI 不直接依赖高德字段层级。
- 导出使用点击时生成的不可变快照，过程中编辑行程不让某张图片混入新旧版本。

## 4. 数据结构详细设计

### 4.1 公共约定

| 项目 | 约定 |
| --- | --- |
| 标识 | 使用稳定、不含个人信息的 UUID；不能用数组下标代替节点身份 |
| 时间长度 | 内部统一为非负整数秒，输入分钟时转换；未知为 null，不用 0、空字符串或 -1 代替 |
| 距离 | 内部统一为米；未知为 null |
| 时间戳 | 保存 UTC 时间戳，面向用户按 Asia/Shanghai 展示，诊断记录同时说明时区 |
| 出游日期 | 日期与当地出发时刻分开记录，用明确的时区组合，不依赖浏览器所在时区 |
| 坐标 | GCJ-02，经度在前、纬度在后；未定位与未确认入口分开标识 |
| 取整 | 计算使用原始秒，界面分钟向上取整并标“约”；合计先汇总原值再取整，不把显示文本再次相加 |
| 版本 | 存储结构使用 schemaVersion；行程修改递增 revision；规划输入另带由有效输入形成的 fingerprint |

一次行程按单日出游设计；跨午夜的结束时间需要显式日期，首轮不自动猜测“次日”，不支持跨天优化时提示拆分行程。

### 4.2 Itinerary：行程

| 字段 | 类型/是否允许空 | 规则 |
| --- | --- | --- |
| id、schemaVersion、revision | 标识、整数 | 每次确认的内容修改递增 revision |
| title、city | 文本，可为空草稿 | 标题不参与地图检索或排序，导出时为空可显示“未命名行程” |
| travelDate、departureLocalTime | 日期、时刻，可空 | 不完整时不显示精确到达时间 |
| timezone | 文本 | 首轮固定 Asia/Shanghai |
| places | 以地点 id 为键的 PlaceRef 集合 | 所有 placeId 必须引用此集合中的对象，不能产生悬空引用 |
| origin、destination | 端点对象，可空 | 各含独立 id、placeId、role；role 分别为 origin/destination，自动规划前必须确认 |
| nodes、nodeOrder | 景点/休息节点集合、标识序列 | 显式保存当前顺序和跳过状态，不通过缺失数组项表示跳过 |
| facilities | FacilityRecord 集合 | 保存设施与地点、节点或路段的关联，不使用没有归属的孤立 Fact |
| legs | 路段集合 | 用起止逻辑节点身份及坐标版本关联，不能复用错误端点的数据 |
| constraints | 约束对象 | 未设置的上限为 null，不代填“老年人标准值” |
| updatedAt | 时间戳 | 本地保存、预览版本和导出快照可追踪 |

路段端点引用 origin.id、destination.id 或 nodes 中的 id；有效路线序列由起点、未跳过的节点和终点组成。同址往返可共用一个 placeId，但起点、终点角色 id 不同。

起终点不重复计为景点停留；同一地点既是景点又是终点时只计算一次景点游览。仅在相邻逻辑节点明确引用同一个、已确认入口的 PlaceRef 时，二者之间可生成标注为同入口衔接的零时长边，不请求高德；园内游览仍独立计算。两个不同 PlaceRef 仅因坐标相同不能自动合并或断言零步行，需用户确认是否为同一入口。

### 4.3 PlaceRef、VisitNode 与 RestNode

| 对象 | 关键字段 | 约束 |
| --- | --- | --- |
| PlaceRef | id、name、providerPoiId、location、coordinateRevision、entranceConfirmed、openingDescription、openingSchedule | 地点名不等于入口已确认；坐标修改会使相关路段失效 |
| VisitNode | id、placeId、required、skipped、visitSeconds、insideWalkSeconds、activityPlan、notes | 园内步行不得大于停留；必去只能由用户主动跳过 |
| RestNode | id、placeId、restSeconds、seatFact | 独立休息点保留顺序；开放信息引用 places[placeId]，未知是否可坐不能默认当成有效坐休 |
| VisitActivity | id、kind、durationSeconds、seatFact | kind 为 walk/rest/other；由所属 VisitNode.activityPlan.items 按顺序持有 |

activityPlan 包含 mode（aggregate/timeline）、completeness（complete/incomplete）及有序 items；aggregate 模式不安排园内坐休，使用已填写合计，timeline 模式使用活动序列并派生合计。完整活动序列的全部时长之和等于 visitSeconds，walk 项之和等于 insideWalkSeconds；不完整则保留缺失项，不以旧合计冒充已精确分段。

openingDescription 为 Fact<string>，保存原始营业描述；openingSchedule 为 Fact<OpeningSchedule>，后者包括适用日期、时区和明确的起止时间窗数组。已核对为空数组表示该日不开放，value 为 null 表示未知；自由文本未核实前不能自动填入结构化窗口。

简单合计模式中，“没有安排园内坐休”是行程安排，不是声称园内不存在座椅；只有明确安排的休息才可影响连续步行计算。

### 4.4 Fact：逐属性事实

每个设施属性独立记录，不把一个地点的存在当成所有属性都可靠。

| 字段 | 规则 |
| --- | --- |
| value | 属性值或 null；明确 false 与未知 null 区分 |
| sourceType | user、amap 或 unknown |
| sourceName、sourceReference | 来源名称及可核对引用；不存带 Key 的上游请求地址 |
| fetchedAt | 数据获取时间，可空 |
| reviewState | unknown、reported、userChecked |
| checkedAt、applicableDate | 用户核对时间与适用日期，可空 |
| note | 限制说明，如“营业时可用”“仅楼梯到达” |

属性例：厕所位置、厕所开放时段、座椅可用情况、台阶存在情况分别对应不同 Fact；reported 不自动转为 userChecked。

FacilityRecord 含 id、kind（toilet/rest-candidate/stairs）、placeId（可空）、target 和 facts；target 明确为 place/node/leg 之一，分别引用地点 id、节点 id 或路段 id。厕所、座椅等地点设施关联 PlaceRef，路段台阶关联具体有向路段，路线重排后不得把旧路段提醒移到另一条路上。

### 4.5 Leg：路段

| 字段 | 规则 |
| --- | --- |
| id | 稳定路段标识；端点、地点引用或坐标版本变化时创建新标识，旧设施关联不能直接转移 |
| fromNodeId、toNodeId | 有向端点身份，A→B 不自动复用 B→A |
| fromPlaceId、toPlaceId | 记录计算时的地点引用，避免换地点但坐标版本相同导致错误复用 |
| fromCoordinateRevision、toCoordinateRevision | 端点坐标变更后的失效依据 |
| mode | walking 或 manual-transfer；首轮不自动优化公交换乘 |
| distanceMeters | 上游返回或手动填入，未知为 null |
| rawWalkingSeconds | 地图基准值或手动原值，单位秒 |
| effectiveWalkingSeconds | 根据适用调整方式派生，不独立编辑 |
| totalTravelSeconds | 步行加接驳等该路段总耗时；不得小于步行部分 |
| durationSource | amap、adopted、manual 或 same-entrance；adopted 表示用户主动采纳的地图估算，same-entrance 仅用于已确认同一入口的零衔接 |
| adoptedAt | 用户采纳时间，仅 durationSource 为 adopted 时存在；用于来源文案与卡片上的时效说明 |
| fetchedAt、providerApiVersion | 时效与来源标识；非高德数据不伪造接口版本 |
| state | missing、loading、ready、stale、unreachable、failed |
| failureCode | 归一化错误码，不含 Key、完整上游地址或堆栈 |

步速因子仅作用于地图基准步行时间；手动填写与已采纳的步行时间视为用户估计，不再乘因子。地图来源的原始值在会话内保留，以便变更因子后重新计算而不重复累乘；采纳后该路段不再随因子变化，如需按新因子调整，应先恢复为地图值并联网重新获取。

采纳值取采纳当时的 effectiveWalkingSeconds，即已按当前因子调整后的整数秒，因此采纳与因子变更的先后顺序会影响结果；卡片上的时间始终标“预计”。

纯步行路段的 totalTravelSeconds 始终等于调整后的 effectiveWalkingSeconds；变更步速后，到达时刻、开放时间与截止时间一并重算。manual-transfer 由用户分别填写总耗时和其中步行耗时，两者不乘地图步速因子，且总耗时不得小于步行部分；未填总耗时不能从步行部分推断完整接驳时长。

### 4.6 Constraints 与 PlanningResult

Constraints 包括：总步行上限、连续步行上限、最低坐休时长、最晚结束日期时刻、步行时间调整因子，以及用户明确设置的通行要求。

- 上限可为空；设置后为硬性限制，不能自动增加。
- 步行因子必须是有限正数，面板说明它只调整估算，不代表体力评估。
- 未设置最低坐休时长时，只把用户明确安排、时长大于零且已确认可坐的休息作为计划分界，不声称休息后一定恢复体力。

PlanningResult 至少包含：resultId、inputFingerprint、节点顺序、采用的路段、统计、每项约束判定、缺失项、冲突、候选差异、搜索范围、搜索完整度和生成时间。

约束判定统一为 PASS、FAIL、UNKNOWN、NOT_SET；单项计算通过不等于整条路线无障碍。

## 5. 时间、步行与可信状态计算

### 5.1 行程用时

1. 累加各实际采用路段的总耗时。
2. 累加景点停留时长和独立休息节点时长。
3. 已知需要等开门时，将等待计入总用时。
4. 景点停留已包含园内步行和该景点内休息，不再次相加。
5. 有任何必需时长未知时，输出已知用时和缺失项，不生成精确结束时间。

开放时间只在明确适用于出游日期且结构化可解释时用于检查；自由文本、节假日可能变化的普通营业描述不得被静默解析成确定时间窗。

### 5.2 总步行与连续步行

- 总步行＝有效路段步行＋园内步行；绕路必须通过真实路段计入。
- 连续步行是两次满足条件的计划坐休之间的累计步行，不等同于钟表经过时间。
- 站立参观、拍照和未确认有座位的乘车不默认重置。
- 休息点是否满足条件与路线是否能抵达该点分别校验。
- 园内有休息但分段位置未知时，不使用一个合计猜测最长连续步行。

### 5.3 缺失数据与下界

统计返回完整值或 null，同时可返回 knownSubtotal、missingFields 和用于判定的下界。

- 总步行的下界可由所有已知非负步行时长相加得出，未知项仍单独展示。
- 已知下界已经超过上限，即使其他数据未知，也可以判定该项 FAIL。
- 连续步行下界必须考虑未知休息可能打断步行：对可能有效但未核实的休息按“可能分界”切分下界，不能把前后相加后断言确定超限。
- 对未知园内活动序列，不把园内合计直接当成一段确定连续步行；仅在确定没有有效休息的已知区间内形成可靠下界。
- 下界没有超限但关键数据缺失时为 UNKNOWN，不能据此 PASS。
- 未设置的限制为 NOT_SET；卡片仍展示已知负担，不写“已符合你的全部要求”。

### 5.4 卡片状态合成

优先检查结构性无效输入，再检查确定超限，再收集缺失与未核验项；不同问题同时保留，不互相覆盖。

| 状态 | 预览与导出 |
| --- | --- |
| 无有效景点或结构性无效 | 显示原因，阻止正常导出 |
| 已知超限 | 可导出带明确冲突提示的草稿，不作为推荐结果 |
| 信息待确认 | 可导出草稿，缺失与来源提示随图保留 |
| 数据完整且无已知冲突 | 正常导出，时间仍标预计，来源与生成时间保留 |

## 6. 自动规划算法详细设计

### 6.1 搜索边界

- 首轮最多 6 个景点。
- 自动规划最多 12 个活动节点，含起点、终点、独立休息点；在最宽松情况下对应 6 景点、2 起终点、4 独立休息点。
- 超出自动规划节点上限时不丢弃节点，提示改用手动整理或拆分行程；这是工程容量限制，不是用户体力限制。
- 用户已安排的独立休息点保留为固定顺序锚点；所属区间内景点可重排，可选景点可省略，不跨休息锚点自动搬移。
- 新搜索到的设施仅作为候选，用户选中并补充必要信息后成为节点，再进行规划；首轮不自动把未核实座椅插进路线当作已确定休息。
- 不把“未找到当前约束内方案”解释为不存在任何城市路线，也不声称本算法求得不限范围的全局最优。

### 6.2 输入预处理

1. 校验必需地点、入口和坐标，移除的只是用户已明确跳过节点，而不是未知信息节点。
2. 由当前顺序确定休息锚点和各景点所属区间。
3. 构建必去集合与可选集合；无必去且全部可选时，仍要求最终至少包含一个景点。
4. 按区间生成可能用到的有向路段，不请求进入起点、从终点出发或跨固定锚点的无效边。
5. 形成输入快照与 fingerprint，再请求缺失路段。

### 6.3 路段查询数量

某区间含 k 个景点时，候选有向边上界为 k²＋k＋1，包含区间起点直达终点的空区间情况；不把所有排列逐个发送给高德。

6 个景点、4 个休息锚点划成 5 个区间时，最坏上界为 36＋6＋5＝47 条不同候选边；同地点、不能省略必去等情况还可减少实际请求。

这是当前受限模型的请求数估算，不代表高德授权配额；适配层仍设置请求预算，并按核实的账号限制节流。

### 6.4 候选枚举与剪枝

1. 枚举各区间内可选景点子集，必去全部包含。
2. 枚举所选景点顺序，连接固定锚点。
3. 按有向路段、景点停留及休息顺序推进时间。
4. 检查开放时间、总步行、连续步行和截止时间。
5. 对已经确定违反硬性限制的分支提前停止，不对未知数据使用零成本剪枝。
6. 将可核验通过的候选与待核验候选分开收集，后者不进入“满足要求”的排名。

无固定锚点、6 个景点全部可选时，含空序列的排列子集总数为 1957；排除空行程后至多 1956 个候选。休息锚点只限制可排列范围，不增加景点数量。

### 6.5 排序与输出

满足可核验限制的候选按下列元组作确定性比较，不混用未经说明的权重评分：

1. 保留的可选景点数量降序。
2. 总预计步行秒数升序。
3. 最长连续步行秒数升序。
4. 总行程用时升序。
5. 原始节点顺序作为稳定平局规则。

输出一个主要建议；若有非重复的备选，可再展示最多两个。没有足够不同方案时不凑“轻松版/经典版”数量，也不随意为方案赋予体力等级。

完整数据的候选优先，待核验候选单独显示已知部分和未知原因；未知时间不能因为数值缺失而被当作更短路线。

### 6.6 部分查询失败与终止

- 明确不可达的边不参加可行路线组合。
- 请求失败、未查询或字段缺失不等于不可达。
- 部分边失败但仍有可核验候选时，可展示候选，并标明搜索数据不完整、比较范围受限。
- 因数据缺失而无法排除潜在方案时，不给出“所有方案均不可行”的确定结论。
- 超出预算或用户取消时停止后续请求，保留已获取结果和原行程，不自动应用半成品。

## 7. 本站 API 契约与高德适配

### 7.1 总体规则

浏览器与服务端采用同源 JSON 请求；服务端只代理设计中列出的高德接口，不接受任意上游 URL、任意请求头或客户端提供的 Key。

所有成功响应包含 requestId、data 和必要的 warnings；错误响应包含 code、message、retryable、fieldErrors 与 requestId。字段错误指向具体字段，不把高德原始错误堆栈传给用户。

### 7.2 内部接口

| 接口 | 请求字段 | 成功响应主要字段 | 边界 |
| --- | --- | --- | --- |
| GET `/api/places/search` | keyword、city、page | items：id、name、district、address、location、entranceStatus；nextPage | 关键词 1～60 字符，每页最多 10 条，用户必须主动确认地点 |
| GET `/api/places/nearby` | longitude、latitude、category、radiusMeters、page | items 及来源、获取时间、属性未知说明 | category 只允许设计白名单；半径设上限，不把候选点直接标成可用设施 |
| POST `/api/routes/walking-matrix` | requestId、inputFingerprint、nodes、pairs | edges、failures、queryCoverage、fetchedAt | 最多 12 节点、64 条唯一有向边；只查询请求明确列出的边 |

内部请求字段补充：

- category 白名单为 `TOILET`、`REST_CANDIDATE`；首轮分别用服务端固定的“公共厕所”“休息区”关键词检索，映射不由客户端任意指定。返回值只是候选，可能有误匹配，不能自动断言有可用厕所或座位。
- nodes 的每项必填 id、placeId、longitude、latitude、coordinateRevision；id 来自起终点或景点/休息节点，最多 64 个字符，在请求内唯一；坐标版本为非负整数。
- pairs 的每项必填 fromId、toId，引用 nodes.id；不接受相同逻辑 id 的自环。同一个已确认入口的零衔接边由客户端领域层生成，不占上游请求。
- requestId 为本次请求关联标识；inputFingerprint 为当前输入指纹，两者都有长度上限且不作为身份认证凭据。
- city 映射到高德 region，可使用已确认城市名或行政区编码；page 从 1 开始，首轮上限 20，每页固定 10 条，不能暗示已返回全城设施。
- 坐标为合法有限数值，纬度和经度范围检查；城市与坐标不符时提示核对，不静默跨城市纠正。
- pairs 引用的节点必须存在，重复边去重；正文限制为 64 KiB。

内部响应字段补充：

- edge 至少包含 fromId、toId、fromCoordinateRevision、toCoordinateRevision、state、distanceMeters、rawWalkingSeconds、provider、providerApiVersion、fetchedAt 和 reportedFeatures；不返回凭据及带 Key 的 URL。
- reportedFeatures 记录“地图报告阶梯”等正向观察及来源，缺失不生成“无阶梯”；需要画线的几何不作为首轮必返字段。
- failure 包含对应 fromId、toId、code、retryable；缺失时长与失败不能转换成零时长 ready 边。
- queryCoverage 为 complete、partial 或 failed：所有请求边均有明确结果时为 complete，其中可包含上游明确报告的不可达；部分边未知或请求失败为 partial；没有任何明确结果为 failed。
- 响应回传 requestId 和 inputFingerprint，前端校验通过才关联到当前候选。
- 周边查询半径默认 500 米、上限 2000 米，这是产品检索范围，不是建议用户实际步行距离；用户确认设施后必须查询实际到达路线。

外部字段映射见第 7.5 节，内部契约不直接暴露高德原始响应层级；外部版本调整只影响适配层。

### 7.3 归一化错误

| 本站错误码 | 场景 | 行为 |
| --- | --- | --- |
| INVALID_INPUT | 参数非法、节点引用不一致 | 不访问高德，定位字段 |
| AMAP_NOT_CONFIGURED | 服务端未配置 Web 服务 Key | 手动功能继续可用，地图功能说明原因 |
| AMAP_AUTH_ERROR | Key 无效、平台不匹配或权限不足 | 不自动重复尝试；提示检查配置，不回显 Key |
| AMAP_QUOTA_EXCEEDED | 配额或频率限制 | 按限制停止或延后请求，不继续刷接口 |
| UPSTREAM_TIMEOUT | 上游超时 | 在预算内有限重试，允许手动继续 |
| ROUTE_UNREACHABLE | 上游明确表示无可用路段 | 区分于网络故障与未知 |
| UPSTREAM_DATA_INVALID | 响应结构或关键数值异常 | 作为失败或缺失，不以 0 替代 |
| REQUEST_CANCELLED | 用户取消或连接关闭 | 停止排队中的上游调用，原行程不变 |

HTTP 200 不等于高德业务成功；适配层还必须检查其业务状态和错误码。语义上的“无路线”可作为单边业务状态返回，不能与整个 API 服务错误混淆。

### 7.4 调用控制

以下数值是待实施验证的应用侧保守起点，不是高德官方授权值：

- 上游单次请求超时 8 秒；只对网络类短暂故障重试一次，不对无效 Key、无权限和参数错误重试。
- 最大并发 2，应用总发送速率默认不超过每秒 1 次；如果核实的账号配额更低，取更低值。
- 一次矩阵任务最多 96 次上游尝试，含重试，整体期限 120 秒；预算用尽返回 partial，不伪造完整结果。
- 单实例同时限制请求队列和总调用量；公开部署前必须确认账号配额、接口权限及服务端访问限制。
- 用户侧始终显示实际阶段，不伪造百分比；关闭请求不代表已经发生的上游调用能退还配额。
- 首轮仅在一次计算内去重和复用数据；跨任务缓存及原始数据保存期限以供应商许可为前置条件，不假定可永久缓存。
- 日志仅记录操作类型、耗时、归一化错误、请求关联标识；默认不记录完整坐标、行程正文或上游查询串。

### 7.5 高德 v5 接口映射

依据 2026-09-22 核对的高德官方公开文档，P1 统一选择 v5 地点搜索和路径规划；本次核对未使用用户 Key，不能视为该账号已获接口权限。官方参考链接见第十六章。

上游主机固定为 `https://restapi.amap.com`，Key 只由服务端附加。

| 用途 | 高德路径 | 关键请求字段 | 归一化规则 |
| --- | --- | --- | --- |
| 地点文本搜索 | `/v5/place/text` | key、keywords；region、page_size=10、page_num；需要营业描述时 show_fields=business | 基础 POI 的 id、name、typecode、location 等映射到地点候选；无入口证据时 entranceStatus 仍待确认 |
| 周边设施候选 | `/v5/place/around` | key、location；固定 keywords；radius、page_size=10、page_num、show_fields=business | location 按经度、纬度发送；返回 distance 只是距查询中心的距离，不能作为步行耗时依据 |
| 两点步行 | `/v5/direction/walking` | key、origin、destination、show_fields=cost | 读取 route.paths 中可用路径的 distance 和 cost.duration，单位分别为米、秒 |

重要版本差异与处理：

- v5 搜索使用 region、page_size、page_num、show_fields，不使用 v3 的 city、offset、page、extensions 参数组合。
- v5 搜索 page_size 的官方范围为 1～25；本站选择 10。官方说明同参数最多可获取 200 条检索结果，因此不得承诺全量设施覆盖。
- v5 周边 radius 的官方范围为 0～50000 米；本站 500/2000 米默认值与上限是产品策略，不冒称上游限制。
- 营业描述位于 business.opentime_today、business.opentime_week 等字段，需要请求 business 扩展；这些描述不是经过核对的未来节假日开放承诺。
- v5 步行总耗时从路径的 cost.duration 读取，不按 v3 的路径顶层 duration 读取；数值字符串要验证后转换，不能把空串强制转换为 0。
- 首轮不请求 polyline 几何；需要绘线时才请求相应扩展。步骤若返回 walk_type=20，可记录为“高德标注阶梯”，不得用未返回该值证明没有台阶。
- 多路径存在时，在字段有效且适用于当前步行请求的结果中选最短预计耗时；仍需执行本产品的设施与约束检查，不能以高德推荐顺序替代无障碍判断。
- 高德业务成功需要检查 status 与 infocode，按文档成功值 1、10000 验证；HTTP 200 但业务错误仍按错误处理。
- 10003 为日配额超限、10004 为访问过频的典型错误，其他鉴权、权限和参数问题按官方错误码表映射，不把未知错误都当网络重试。
- 响应字段可能缺失或呈现不同空值形态；只对预期字段做显式归一化，缺失、空字符串、空数组均不作为有效数字零。响应结构整体不符时记为上游数据异常。
- 官方 v3 步行页明确出现过 100km 上限；本次未在 v5 步行页核实相同限制，禁止将旧版值当作 v5 保证，接入时按实际授权与错误反馈处理。
- 步行接口未提供本产品所需的多景点全行程优化；本系统必须通过有向两点路段和本地候选计算完成。

### 7.6 坐标、权限与设施边界

- 高德地图坐标按 GCJ-02 处理，发送时经度在前、纬度在后，小数按官方要求控制在不超过 6 位；同一地点的规范化坐标用于版本与路段关联。
- GPS/WGS-84 坐标若以后进入系统，需明确转换流程；已有高德坐标不能再次转换，首轮不自动请求实时定位。
- Web 服务 Key 应对应高德控制台“Web 服务”类型；JS API 使用独立平台 Key，新申请的 JS Key 按其规则配置安全码，不能把 securityJsCode 当成本次 REST 接口参数。
- 配额、QPS、商业使用及存储展示限制需要依据账号控制台与服务条款核实，不以文档中的示例 Key 或统一免费额度作为上线依据。
- 未发现官方对座椅覆盖、设施实时开放、所有台阶或无障碍通行完整性的保证；相关事实继续遵循未知状态和逐项核对规则。

## 8. 交互状态与并发一致性

### 8.1 三类独立状态

| 状态域 | 状态示例 | 禁止的混淆 |
| --- | --- | --- |
| 存储 | dirty、saving、saved、saveFailed | saved 不表示路线满足要求 |
| 数据请求 | idle、loading、partial、success、failed、cancelled | success 不表示设施已核对 |
| 路线内容 | invalid、violated、needsReview、ready | ready 仍是估计，不承诺实际安全 |

编辑字段的无效文本保留在表单草稿中，不用旧有效值假装新输入已经保存；导出前检查是否仍存在未处理的无效输入。

### 8.2 状态转换

- 确认有效修改 → 更新领域数据与 revision → 计算输入 fingerprint → 使相关路段或规划结果失效 → 刷新统计 → 自动保存。
- 只有标题等不影响路线的字段变化，不重新请求地图；是否仍可应用规划结果按有效输入 fingerprint 判断，而非只看完整文档 revision。
- 请求开始记录 requestId 和 fingerprint；响应及 Worker 结果必须同时匹配当前请求和输入后才能进入可应用状态。
- 新请求开始或用户取消后，旧响应只可被丢弃，不得覆盖当前错误、结果或行程。
- 用户确认候选时再次检查 fingerprint，检查失败则要求重算。

### 8.3 跳站与恢复

1. 只改变节点 skipped 状态，保留原资料。
2. 重建当前活动序列，找出新增和移除的邻接关系。
3. 沿用端点身份、地点引用、坐标版本、提供方等仍匹配且允许复用的路段；其余转为 missing/stale，并按路段身份规则重建关联。
4. 统计立即反映“部分已知/待更新”，不显示旧的完整总量。
5. P0 引导填写新路段；P1 请求重算并重新检查时间与休息。
6. 恢复节点执行同一规则，不按旧数组位置取回时间。

已采纳的路段属于用户数据，端点身份与地点引用仍匹配时优先复用，不因其来源为地图估算而失效；端点变化时与其他路段同样转为 missing，不用旧时间顶替新路段。

### 8.4 本地保存

- 建议存储键为 `easywalk.current-itinerary`，结构中含 schemaVersion。
- 主编辑区合法字段确认后自动保存；局部面板取消不触发对原对象的写入。
- 有效输入可采用短延迟合并写入，失焦或确认时及时落盘；不要依赖关闭页面事件完成所有保存。
- 存储损坏、浏览器禁止写入或容量不足时保留内存内容并提示；未经用户确认不清空存储。
- 未识别的 schemaVersion 不静默覆盖；首轮不实现推测性的多版本迁移。
- 用户手动数据可以本地保存；地图来源数据的持久化白名单与采纳机制见第 8.5 节。未经用户采纳的地图值不写入本地存储，也不能以用户“选中了地点”自动推定获得数据再分发权。
- 上述地图字段限制不影响 P0 手动行程刷新恢复。
- 不承诺跨设备或多标签协作；检测到其他标签页修改时，不静默用外部内容覆盖正在编辑的草稿。

### 8.5 地图来源数据的采纳与离线可见

**问题**

第 8.4 节在许可未核实前不持久化高德返回的路段数值，重载后相关路段回到待获取。但需求第 1.3 节与第 4.4 节的途中调整要求行中可用，而景区常处于弱网环境：若每次切回页面都必须联网重取，安排者在途中既看不到步行时间，也导不出可用的路线卡。

**处理原则**

地图返回的步行时间是外部来源数据，不能因为用户选了这个地点就推定可以持久化或再分发；但用户可以主动把它采纳为本行程自己的估计，此后该数值按用户数据保存。采纳是显式操作，不做静默转换，也不把采纳包装成核实地图数据。

**采纳机制**

- 入口：当行程中存在 durationSource 为 amap 且已就绪的路段时，在编辑视图显示一条非阻塞提示，说明这些时间来自地图查询、仅当前会话有效，并提供“采纳全部路段时间”操作；不放进导出流程，也不做成含义不清的全局“保存并完成”。
- 效果：被采纳路段的 durationSource 变为 adopted，写入 adoptedAt，随行程保存到本机；重载后保持 ready，可离线查看与导出。
- 值：采纳当前 effectiveWalkingSeconds，即已按当时步速因子调整后的整数秒；采纳后该路段不再随因子变化。
- 覆盖：采纳后重新获取地图值时，用新的地图值替换该路段有效值并清除采纳标记；界面提示“已更新为地图值，刷新后需重新获取”，避免同时存在两个值。同一路段始终只有一个有效值，不产生第二份事实。
- 恢复：提供“恢复为地图值”，恢复后该路段回到待获取，需联网重新获取；不保留已采纳值作为另一份可对照的数据。
- 采纳不代表实时有效：卡片与来源文案必须同时给出采纳时间与“非实时”提示。
- 与规划重算的关系：已采纳路段作为有效路段参与重算，不重复请求上游，也不在重算中被隐式替换为新的地图值；需要按最新地图数据调整时，由用户先执行“恢复为地图值”。这一取舍避免在线时的重算静默覆盖用户的离线数据。

**持久化白名单**

| 数据 | 是否持久化 | 说明 |
| --- | --- | --- |
| 已采纳路段的 effectiveWalkingSeconds、totalTravelSeconds、adoptedAt、durationSource=adopted | 是 | 采纳后属用户数据 |
| 用户手动填写的路段与节点数据 | 是 | 第 8.4 节既有规则 |
| 用户主动选择的 PlaceRef（名称、坐标、providerPoiId、coordinateRevision、entranceConfirmed） | 是 | 行程的组成部分；没有它行程无法显示或计算 |
| 用户填写或核对过的设施属性与核对日期 | 是 | 第 4.4 节 Fact 中 reviewState 为 userChecked 或由用户直接填写的值 |
| 高德原始响应、rawWalkingSeconds、distanceMeters、providerApiVersion | 否 | 未经采纳的源数据不落盘 |
| 高德返回的失败与不可达状态、failureCode | 否 | 重载后统一回到待获取；不把一次失败当成永久结论 |
| 高德返回的设施属性原值（reviewState 为 reported） | 否 | 重载后回到待确认；reported 不自动转为 userChecked |
| 地点搜索结果列表与周边候选列表 | 否 | 属于检索缓存而非用户选择，时效与期限以许可为准 |

**重载后的状态重建**

| 该路段在重载前的状态 | 重载后 |
| --- | --- |
| durationSource 为 adopted 且已就绪 | 保持 ready，可直接查看与导出 |
| durationSource 为 amap 且已就绪，但用户未采纳 | 回到 missing；按第 5.3 节计入缺失项，合计显示已知部分 |
| unreachable 或 failed | 回到 missing，不沿用上次的失败结论 |
| same-entrance 零衔接 | 由领域层按已确认的同一入口重建，不依赖持久化 |

**卡片与导出的体现**

- 来源块必须区分来源：地图来源与已采纳来源分别标注，已采纳来源写明采纳时间。
- 未采纳路段在重载后回到待获取时，卡片按第 5.4 节标为草稿并显示“已知步行约 X 分钟，另有 N 项待补充”，不显示旧完整合计。
- 导出不得把上次会话的地图数值当作当前结果；生成时间随图保留。
- 行中跳过景点产生的新路段没有可用来源，按第 8.3 节回到待获取，可联网获取或手动填写；不得用旧路段时间顶替新路段。

**许可核实后的简化路径**

若后续核实高德许可允许在一定期限内存留并在同一行程内复用路段数值，可直接持久化 amap 来源及其获取时间，省略采纳步骤，并相应更新本节与第 8.4 节。在许可未核实前不采用该路径，也不以“用户已采纳”推定获得再分发权。

## 9. 手机、平板、电脑详细界面设计

### 9.1 通用视觉规范

颜色、字阶、圆角、间距和状态令牌沿用需求说明书第 13.5～13.7 节，不复制成另一套数值来源。

- 编辑区中性黑灰＋绿色主要操作；路线卡白底深色文字。
- 编辑正文 16px，路线主文字 22px，辅助提醒 18px；触控目标至少 44×44 CSS 像素。
- 线性路线是行程示意，不冒充导航地图；高德是数据提供方，不要求页面首屏显示底图。
- 主要动作、错误和来源不依赖鼠标悬停可见。

### 9.2 响应式决策表

| 可用宽度 | 手机场景 | 平板场景 | 电脑场景 | 布局规则 |
| --- | --- | --- | --- | --- |
| 320～639px | 常见竖屏 | 窄分屏 | 极窄窗口 | 单列、编辑/预览切换、复杂编辑全屏 |
| 640～1023px | 横屏 | 竖屏/部分横屏 | 窄窗口 | 最大宽度 720px 单列，短字段可两列 |
| ≥1024px | 少见宽视口 | 宽横屏 | 常规窗口 | 最大宽度 1200px，编辑＋360px 预览，间隔 24px |

不通过设备型号判断界面；iPad 横屏可以使用双列，但仍是触屏优先控件，电脑窄窗口也必须重排成单列。

### 9.3 手机端

- 顶部显示中文名，英文副标题可单独占行，不把保存状态和多个按钮挤成一行。
- 主区按“基础信息—步行汇总—路线节点—补充操作”排序。
- 固定底部操作为编辑时“预览路线卡”、预览时“导出图片/导出草稿”。
- 表单键盘出现时优先保证输入及确认操作可见，必要时取消吸底；为安全区及工具栏变化留出空间。
- 浏览器返回由预览回到编辑，保留草稿和合理的滚动位置。
- 长地点名换行，关键提醒不折叠；不以左右滑动隐藏删除或恢复入口。
- 大字预览的纸面按第 10.3 节的纸面宽度与缩放规则显示：可用宽度不足时整体等比缩小，不参与窄屏重排，也不产生横向滚动。

### 9.4 平板端

竖屏及窄分屏线框如下，宽横屏复用电脑双列但保留相同触控尺寸。

```text
省脚力路线卡
EasyWalk Route Card                   已保存在本机

[编辑行程]  [大字预览]
┌──────────────────────────────────────┐
│ 行程名称                             │
│ 城市              日期               │
│ 起点              终点               │
├──────────────────────────────────────┤
│ 预计步行 / 连续步行 / 待确认事项     │
├──────────────────────────────────────┤
│ 当前路线节点                         │
│ 路段、休息点、可选景点与编辑入口     │
│                                      │
│ [添加景点]       [添加休息点]         │
└──────────────────────────────────────┘
[预览路线卡]
```

- 横竖屏及分屏变化只改变布局，不重建行程、清空草稿或重复请求地图。
- 弹层中未确认的局部草稿在旋转设备后保留；焦点落在仍可见的当前编辑对象。
- 支持触控、外接键盘和触控板，排序必须有上移/下移操作。
- 约 768px 宽度时采用居中单列，不强塞左右两列造成主文字缩水。
- 当软键盘挤压可用高度时，面板允许滚动并保持关闭/确认可达，不锁死页面。

### 9.5 电脑端

- 编辑列自适应剩余宽度，预览列固定基准 360px；主工作区不超过 1200px。
- 使用页面纵向滚动，不默认创建左右两套独立滚动。
- 节点编辑使用约 480px 侧面板，空间不足时退回全屏编辑，不能覆盖后仍使底层可聚焦。
- 鼠标悬停只作为增强反馈，全部功能可通过点击和键盘完成。
- 3840px 宽屏保持中心工作区和合理留白，不无限放大输入框和拉长文字行。

### 9.6 组件交互契约

| 组件 | 必需输入 | 产生的用户事件 | 不应承担的职责 |
| --- | --- | --- | --- |
| TripEditor | 行程、字段错误、存储状态 | 修改字段、添加节点、预览 | 直接解析高德响应 |
| RouteNodeCard | 节点、关联路段、提醒 | 编辑、跳过、恢复、上移、下移 | 自行重算整条路线 |
| FacilityEditor | 当前逐属性事实和局部草稿 | 确认、取消、核对单属性 | 默认把所有未知改成已确认 |
| PlanningPanel | 条件、请求状态、候选、版本 | 计算、取消、查看差异、应用 | 直接覆写当前行程 |
| RouteCard | CardViewModel（第 10.2 节） | 只读，必要的来源展开仅在网页预览 | 编辑行程、地图请求、自行决定分页 |
| ExportPanel | 行程快照、CardPage（第 10.3 节）、生成状态 | 生成、逐页保存、重试、关闭 | 自动发送到家庭群、改写卡片文案 |

## 10. 图片导出与三端保存

### 10.1 导出流程

1. 校验当前表单是否存在结构性无效输入，提示隐私检查。
2. 冻结当前已应用行程快照，按第 10.2 节生成统一 CardViewModel。预览与导出必须消费同一实例。
3. 在独立纸面渲染区以 360px 逻辑宽度排版，等待字体及布局稳定。
4. 按第 10.2 节的块序列测量每块版面高度。
5. 按第 10.3 节算法分页，保留完整节点与必要承接信息。
6. 以 3 倍像素密度逐页渲染为 PNG，展示生成结果与逐页保存入口。
7. 导出结束、取消或关闭后释放不再使用的画布和对象 URL。

### 10.2 卡片视图模型 CardViewModel

CardViewModel 是领域计算与纸面渲染之间的唯一接口：`domain → route-card → export` 三段只通过它传递内容。网页预览与图片导出消费同一实例，使“预览与导出一致”由结构保证，而不是靠两处人工比对。

| 设计约束 | 说明 |
| --- | --- |
| 纯数据 | 只含字符串、数字、布尔、枚举与数组；不含 DOM 节点、组件元素、函数或领域对象引用 |
| 文本预格式化 | 单位、“约”、待确认与缺失说明在生成时一次确定，渲染层不再二次拼接或补全 |
| 只读快照 | 不读取全局编辑状态、原始高德响应、校验错误对象或未应用的规划候选 |
| 不含操作控件 | 块内不出现按钮、输入与焦点环；来源展开等交互由预览层附加，不进入导出 |
| 单一有序序列 | 全部内容以 blocks 表达，渲染与分页遍历同一序列，不各自维护顺序 |

顶层字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| version | 正整数 | 本模型结构版本，与存储 schemaVersion、行程 revision 相互独立 |
| meta | CardMeta | 行程名称、日期与生成时间 |
| status | CardStatus | 卡片整体状态，决定是否标“草稿”及导出是否被阻止 |
| blocks | CardBlock[] | 有序内容块，唯一的内容来源 |
| footText | 文本 | 页脚模板，导出时按页回填页码 |

CardMeta：

| 字段 | 允许空 | 说明 |
| --- | --- | --- |
| titleText | 否 | 行程名；原名为空时使用“未命名行程” |
| subtitleText | 是 | 城市或副标题；为空时不占行 |
| dateText | 是 | 出游日期文本；未填写为 null，此时不显示任何精确到达时刻 |
| generatedAt | 否 | UTC 时间戳，用于快照追踪 |
| generatedAtText | 否 | 面向用户的生成时间文本，按 Asia/Shanghai 展示 |

CardStatus：

| 字段 | 取值 | 说明 |
| --- | --- | --- |
| kind | complete / draft / violated / blocked | 与第 5.4 节状态合成一致 |
| notices | StatusNotice[] | 每项含 text、severity、anchorBlockId |

kind 的导出含义：blocked 表示结构性无效或没有有效景点，正常导出被阻止；violated 与 draft 允许导出，但状态提示必须随图保留；complete 仍标“预计”，不代表路线已核验。

块公共字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | 文本 | 块标识，同一模型内唯一；用于测量、分页与错误定位 |
| kind | 枚举 | 见块类型表 |
| breakMode | atomic / text | atomic 表示整块不可拆；text 表示自身超过整页高度时可按文本行拆分 |
| stickWithNext | 布尔 | 为真时不得与紧随其后的块分到不同页 |
| sectionTitleText | 文本，可空 | 分组标题，仅在该分组首块上出现 |

块类型：

| kind | 对应内容顺序 | 主要字段 |
| --- | --- | --- |
| header | 需求 R05 第 1 项 | titleText、subtitleText、dateText、generatedAtText |
| summary | 第 2 项 | items：SummaryItem[] |
| node | 第 3 项 | role、indexText、titleText、metaLines、badges、notices |
| leg | 第 4 项 | mainText、secondaryText、state |
| note | 节点补充说明 | ownerNodeId、text |
| notice | 第 5 项 | text、severity、anchorBlockId |
| skipped | 第 6 项 | items：SkippedItem[] |
| source | 第 7 项 | items：SourceItem[]、checkText |

- node 的 role 为 origin / visit / rest / destination；indexText 仅景点有值，起点、终点与独立休息点为 null。
- node 的 metaLines 为已格式化行，如“停留 30 分钟”“园内步行 8 分钟”；数据未知时使用明确的未知文案，不写 0。
- node 的 badges 每项含 text 与 tone（neutral / brand / warning），用于“必去”“可选”“累了可跳过”“已跳过”；tone 不替代文字。
- node 的 notices 承载厕所、台阶等待确认提醒，每项含 text 与 severity。
- leg 的 state 为 ready / missing / stale，是第 4.5 节 Leg.state 的展示投影而非原样拷贝；missing 时 mainText 使用可理解的缺省文案，不得显示为 0 分钟。
- note 是所属节点的补充文本，通过 ownerNodeId 关联；需要按行拆分的文本必须显式声明为 note 块，渲染层不得自行决定拆在哪里。
- summary 的 items 每项含 key（totalWalk / totalDuration / longestWalk / restCount）、labelText、valueText、unitText、state（known / partial / unknown / violated）与 noteText。
- 模型只表达“块是什么”，不表达“块怎么画”；字号、颜色与像素值由需求说明书第 13.5～13.7 节的纸面令牌决定，模型内不出现视觉数值。

### 10.3 分页参数、算法与数据结构

**分页参数**

| 参数 | 值 | 来源 |
| --- | --- | --- |
| 逻辑宽度 | 360 CSS px | 需求 13.9 |
| 内边距 | 16 px | 需求 13.6 |
| 每页逻辑高度上限 | 960 px | 本节 |
| 像素倍率 | 3 | 需求 13.9 |
| 输出宽度 | 1080 px | 360 × 3 |
| 单页输出高度上限 | 约 2880 px | 960 × 3 |
| 单页未压缩 RGBA 画布 | 约 11.9 MiB | 1080 × 2880 × 4 字节 |

960px 是逻辑高度预算，不是设备高度；手机、平板与电脑导出使用同一数值，不随设备字号或视口变化，避免同一行程在不同设备上得到不同页数。

**预览显示宽度与缩放降级**

纸面在预览与导出中使用同一个 360px 逻辑宽度（内容区 328px，内边距 16px×2）。宽度是排版规格，预览只改变显示比例、不改变排版宽度，否则预览将不再预示导出的换行与分页。

| 预览容器可用宽度 | 显示方式 |
| --- | --- |
| 不小于 360px | 按 1:1 显示，纸面居中；桌面预览列基准 360px 即属此类 |
| 小于 360px | 整体等比缩放至可用宽度，缩放比等于可用宽度除以 360，字号、换行与分页均不改变 |

- 大字预览视图的纸面容器左右不做页面留白，留白由卡片自身的 16px 内边距承担；需求 13.8 的“左右间距 16px”适用于编辑视图，以及预览视图中纸面之外的工具栏部分。这样在 360px 视口下纸面可 1:1 显示，满足需求 D05。
- 按此规则，360px 视口下纸面为 360px、主要路线文字为 22px，D05 通过；320px 视口下纸面缩放至约 0.889，无横向滚动，D01 通过。
- 缩放只作用于纸面容器：返回编辑、导出图片等工具栏操作位于缩放容器之外，保持至少 44×44 CSS 像素，且不参与缩放。
- transform: scale() 不改变布局盒尺寸，实现时必须同步修正容器的占位高度，避免出现空白区或末页被裁切。
- 缩放生效时给出简短说明，例如“已按屏幕宽度缩小显示，导出图片仍为大字版”；不能让安排者把屏幕上的显示尺寸误当成图片的实际尺寸。
- 缩放只影响显示，不影响导出：导出始终按 360px 逻辑宽度排版并以 3 倍输出 1080px，与设备、视口和系统字号无关。
- 视口窄于 320px 时按同一公式继续缩放，不承诺可读性，但仍不产生横向滚动。
- 缩放不改变 CardViewModel 与分页结果：块、顺序、页码与续页承接在所有显示宽度下保持一致。

**分页算法**

1. 冻结行程快照，生成唯一的 CardViewModel。
2. 将 blocks 按顺序渲染进与导出共用同一样式作用域的离屏纸面容器：宽度固定 360px，不限制高度，移出可视区。等待 document.fonts.ready 后再测量，字体使用系统中文无衬线字体。
3. 逐块读取布局高度与相邻块间距，得到每块版面高度；测量与导出必须使用同一字体、字号、行高与内边距，不用估算行数代替测量。
4. 计算每页正文可用高度：960 减去页眉与页脚高度；首页另需容纳 header 块。
5. 依序装箱，累计高度超过可用高度时结束当前页并另起一页。
6. 处理 stickWithNext：判断时使用本块与下一块的组合高度，使成对内容不被分页切开；分页点因此落在 node 之前，而不是落在 node 与其 leg 之间。
7. 处理可拆块：若某 breakMode 为 text 的块自身高度大于整页可用高度，则按文本行拆分，前段留在当前页，剩余部分作为同 id、partIndex 递增的新块进入下一页；拆分预算需预留“续”标记所占行高。
8. 得到总页数后回填页码，形如“第 X / Y 张”。
9. 若某页首块不是 header，在其前插入承接标记，引用上一页最后一站的名称，使读者知道本页从哪一站继续。
10. 为每一页生成独立的 360px 容器，只放入该页块，逐页以 3 倍像素密度渲染为 PNG；每页渲染完成后立即释放画布与对象 URL，不同时保留全部大画布。

**文本行拆分**

- 使用 Range 对文本节点二分查找：以不超过本页剩余高度为条件取最大字符偏移，用 getClientRects() 判断该偏移是否落在行边界。
- 拆分点必须落在行边界，两段文本直接拼接应还原原文，作为可回归的断言。
- 中文按 Unicode 码位步进，不拆散代理对与组合字符。
- 不复刻 CSS 换行：不使用 Canvas measureText 自行断行，避免与真实排版结果不一致。
- 不使用缩小字号、裁切、隐藏溢出或省略号作为拆分失败时的替代手段。

**分页数据结构**

| 对象 | 字段 | 说明 |
| --- | --- | --- |
| CardPage | pageIndex、pageCount、blockRefs、continuationLabelText、footText | 一页的完整渲染输入；continuationLabelText 为续页承接信息，首页为空 |
| PageBlockRef | blockId、partIndex、textRange、continuedFromPrevious、continuesToNext | 指向模型中的块；被拆分的块以 partIndex 与字符区间标识 |

分页结果只引用块 id 与字符区间，不复制或改写块内容，因此分页不产生第二份文案。

**输出规则**

- 来源块整张卡片只出现一次，位于全部节点之后；每页页脚重复页码与生成时间。此约定同时满足需求 13.9 的内容顺序与本节的逐页标识要求。
- 不将编辑按钮、浏览器焦点环、隐藏的过期候选或无关弹层渲染进图片。
- 不依赖远程旅游图片或真实地图截图，减少跨域污染、字体及署名问题。
- 被跳过的节点不出现在路线块中；仍参与行程的可选节点保留“累了可跳过”。
- 同一行程的多页图片保持相同宽度、字号、边距与状态色。

**失败与边界**

| 情况 | 处理 |
| --- | --- |
| 字体未就绪或测得高度为 0 | 不生成图片，提示重试；不以空白图当成功结果 |
| 不可拆块自身超过整页高度 | 阻止导出并指出具体块，提示精简该段内容；不裁切、不缩小字号 |
| 某页渲染失败 | 只提示该页，允许重试，已成功页不丢失 |
| 无法确认浏览器已落盘 | 只提示“图片已生成”，不提示“已保存到相册” |
| 导出过程中修改行程 | 当前快照不变，改动只影响下一次导出 |

### 10.4 保存行为

- 电脑优先提供下载；手机和平板同时提供可打开的图片及逐页保存说明。
- 不保证浏览器允许一次触发多文件下载，不误报“已保存到相册”。
- 浏览器支持系统分享且已完成兼容性测试后，才增加显式分享操作；初版图片生成不等于已发送。
- 导出文件名使用安全清理后的行程名、日期和页序，不包含 Key、完整地址或无关个人信息。
- 导出中编辑的内容只影响下一次导出，当前快照继续保持一致。

## 11. 高德凭据、隐私与安全设计

### 11.1 凭据处理

- 已收到用户提供的高德 Key，但本轮不保存其原值、不调用验证、不写入文档、示例或 Git。
- 不能仅凭字符串判断是 Web 服务 Key 还是 JS API Key；进入接入阶段前确认平台类型和已授权接口。
- 后续服务端配置名称约定为 `AMAP_WEB_SERVICE_KEY`，只从服务端环境读取。
- 本地开发配置文件必须被 Git 忽略，示例仅包含空值或占位说明；不得使用前端可公开打包的环境变量承载该凭据。
- 当前设计不需要 JS API Key 或 securityJsCode；以后增加底图时单独按高德要求配置，不混用。
- 若真实 Key 被写入共享文档、公开代码或日志，必须先停用/轮换，再清理暴露内容；不能仅删除最新文件后认为历史已安全。

### 11.2 服务边界

- 固定上游域名与路径，服务端不实现任意 URL 代理。
- 查询字段、坐标、节点引用、页码、正文体积和请求预算均在服务端验证。
- 同源与 CORS 是浏览器边界，不是防止外部盗刷的充分措施；公开部署还需总配额、请求频率、队列上限及必要的访问控制。
- 不将整个高德响应原样记录到日志，不泄漏凭据和出行信息。
- 用户备注按普通文本呈现；来源链接如允许展示，只允许明确的安全协议，不执行用户输入。
- API 对行程相关响应采用不落浏览器共享缓存的策略，避免私人坐标被公共缓存复用。

### 11.3 本地数据与发布边界

- 本地浏览器存储不等于加密保险箱，也不等于跨设备备份。
- 三端适配表示功能与排版兼容，不承诺行程在三台设备之间自动同步。
- 不默认定位或上传路线图片；公开分享由用户自行决定。
- 本轮不创建部署、远端仓库、账号或数据库；发布前重新确认访问策略与高德许可。

## 12. 性能、错误恢复与可观察性

| 环节 | 设计目标/边界 | 失败恢复 |
| --- | --- | --- |
| 输入与本地预览 | 典型 6 景点下交互目标不超过 300ms | 不让地图请求阻塞表单 |
| 本地候选计算 | 在 Worker 执行，典型输入目标 1 秒内；须按约定设备验证 | 取消或异常保留原行程，不冻结主线程 |
| 地图查询 | 受真实账号配额及网络限制，展示实际阶段；不保证固定几秒完成 | 达到期限返回 partial/failed，允许重试或手动继续 |
| 保存 | 正常修改及时保存，错误明确可见 | 保留内存，不清空草稿 |
| 导出 | 逐页生成，限制同时存活的大画布数量 | 哪页失败提示哪页，重试不重新丢失编辑内容 |
| 字体与三端排版 | 系统字体优先，布局可测量 | 不用空白字形图片当成功结果 |

记录的数据应限于耗时、计数、状态及错误分类；未获需要和授权时不建设用户行为画像或上传完整行程的统计系统。

## 13. 验证设计与需求追踪

### 13.1 测试层次

- 领域单元测试：未知/零/无效值、重复计算、连续步行、时间窗、候选排序、路段失效和稳定平局。
- 组件测试：表单标签、局部取消、保存反馈、草稿与超限标识、焦点恢复。
- 高德适配契约测试：成功、业务失败、空结果、字段缺失、数值字符串、限流及超时；模拟数据必须标明来源与性质。
- 浏览器测试：编辑—跳站—重算—预览—导出，取消与失败路径，三端断点及实际保存。
- 接入阶段真实核验：在确认 Key 平台、授权和配额后，用少量公开地点检查接口契约；不能把模拟成功当成真实接入通过。

### 13.2 关键验证用例

| 编号 | 验证内容 | 对应需求 |
| --- | --- | --- |
| T01 | 统一样例总步行 43 分钟、用时 100 分钟、连续最多 28 分钟 | R04、A01、需求 10.3 |
| T02 | 缺少 A 园内步行后显示已知 35 分钟＋1 项待补充，而非完整总量 | A05、D07 |
| T03 | 未知休息可能打断连续步行，不错误断言确定超限 | R04、B03、D08 |
| T04 | 已知下界超限且另有未知项时仍提示确定冲突 | B02、D08 |
| T05 | 跳过中间节点后不能用原两段相加作为新直达时间 | R07、A04、B04、D10 |
| T06 | A→B 与 B→A 不混用；入口坐标修改后相关边失效 | R02、B07 |
| T07 | 手动时间不重复乘步速因子，地图原始值不被覆盖 | R04、需求 6.1、详细设计 4.5 |
| T08 | 所有必去和固定休息锚点保留，可选至少剩一个有效景点 | B01、需求 6.3 |
| T09 | 缺失时间的候选不以零成本排名第一 | B03、详细设计 6.4 |
| T10 | 地图 HTTP 成功但业务失败，不当成有效路线 | B05、D10 |
| T11 | 不同请求乱序返回及计算中改标题/改坐标，分别正确复用或失效 | D09、需求 14.5 |
| T12 | P0 没有 Key 时手动功能完整，地图入口不冒充已可用 | D15 |
| T13 | 局部面板取消不修改原行程；非法输入不静默丢失 | A07、D11 |
| T14 | 导出期间修改行程，图片来自单一快照 | R06、D13 |
| T15 | 多页内容、来源、草稿和超限提示保持完整 | A08、D13 |
| T16 | 三端图片生成与设备保存结果区分 | D14 |
| T17 | 平板旋转、分屏和外接键盘不丢草稿与焦点 | D17 |
| T18 | Git 暂存内容、构建产物和浏览器请求均不含真实服务端 Key | 详细设计 7.3、详细设计 11.1 |
| T19 | 查询预算耗尽返回 partial，不声称全搜索不可行 | B08、需求 6.4、详细设计 6.6 |
| T20 | 暴露日志、错误正文及请求参数不包含凭据或无关行程数据 | 详细设计 7.3、详细设计 7.4 |
| T21 | 320、360、390px 视口查看大字预览并导出 | 360px 视口下纸面 1:1、正文 22px；320px 下等比缩小且无横向滚动；工具栏不参与缩放；同一行程在不同视口导出得到相同页数与块顺序（D01、D05、D17） |
| T22 | 采纳地图路段时间后重载页面并离线查看与导出 | 行程、顺序、备注、跳过状态与已采纳路段全部保留；未采纳路段回到待获取且合计只显示已知部分；失败状态不被当作永久结论；卡片区分来源并标注采纳时间与“非实时”（A02、R01、R07、需求 7.1、详细设计 8.5） |

本表引用约定：R、A、B、D 编号均来自需求说明书；章节号必须标明所属文档（需求 / 详细设计），因为两份文档存在相同的章节号但内容不同。

T08 中“至少一个景点”针对没有必去景点且全部可选的情况；有必去时自然由必去满足，不能强制额外增加可选景点。

### 13.3 三端验收矩阵

| 终端 | 最少覆盖视口/场景 | 浏览器与输入方式 |
| --- | --- | --- |
| 手机 | 320、360、390px；横屏；软键盘；安全区 | Android Chrome、iOS Safari；触摸和读屏 |
| 平板 | 768×1024、820×1180、1024×768、1180×820；512px 分屏 | iPadOS Safari、Android 平板 Chrome；触摸、键盘和触控板 |
| 电脑 | 1024、1440、1920、3840px 宽；200% 缩放；窄窗口 | Chrome、Edge；键盘与鼠标 |
| 内嵌浏览器 | 声明支持微信保存时必须单独覆盖 | 实际设备验证，不能只用桌面模拟视口替代 |

测试时记录实际浏览器与系统版本；当前表为计划，不表示已测试或对所有历史版本作兼容承诺。导出与图片保存相关的项无法由自动化测试替代，须按第 14.2 节在真实设备上人工验证。

## 14. 后续开发与 Git 分步骤提交规范

**本章是未来执行要求，不授权本轮初始化仓库、修改 Git 配置、暂存、提交或推送；本轮仅编写本说明书。**

### 14.1 仓库管理原则

- 后续进入实际代码开发阶段时，所有源代码、测试、必要文档及依赖锁文件纳入 Git 管理。
- 先检查工作区是否已有仓库、是否有用户未提交改动；已有仓库沿用既有约定，不覆盖用户工作。
- 新仓库的位置和分支需在启动开发时说明；没有远端时明确告知，不自行配置远端或推送。
- 本地环境文件、真实 Key、日志、依赖目录、构建产物及私有截图应按实际项目情况加入忽略规则；可提交的环境示例不得含真实值。
- 每步先验证，再按明确文件路径暂存，核对暂存差异及敏感信息后提交；不使用包含未知文件的全量暂存。
- 不跳过提交钩子，不擅自修改 Git 用户配置，不用强推或改写历史作为排障捷径。

### 14.2 实施前的导出技术验证（Spike）

**为什么放在最前**

图片导出是 P0 中唯一没有既定实现方案的能力：第 2 节的选型与第 10.3 节的算法都建立在尚未实测的假设上。若按提交顺序到第 5 步才首次验证，一旦 `html-to-image` 在 iOS Safari 或微信内嵌浏览器上无法稳定渲染中文长文本、无法按 3 倍像素密度输出，或大画布触发移动端上限，第 4 步的纸面组件会连带返工。

因此把“能不能做”与“做完整”分开：先用一次临时验证回答可行性问题，再进入第 1 步。验证产物是可复现的结论与最小样例，不是产品代码，也不需要领域模型或界面完成。

**验证项与处置**

| 验证项 | 通过标准 | 不通过时的处置 |
| --- | --- | --- |
| 输出尺寸与像素密度 | 360px 逻辑宽度、pixelRatio 3 输出精确 1080px 宽，元素无错位 | 调整渲染管线；仍不通过则改用 Canvas 渲染器 |
| 中文长文本完整性 | 6 个景点、多个休息点与超长中文备注下无缺字、无裁切、无换行错乱 | 排查字体与节点克隆方式；仍不通过则改用 Canvas 渲染器 |
| 系统字体可用 | 苹方、微软雅黑及系统 sans-serif 在 iOS Safari、iPadOS Safari、Android Chrome 正常渲染 | 调整字体族顺序；不改为远程字体，需求 13.6 要求不依赖外部字体才能导出 |
| 单块版面高度测量 | 离屏容器中可稳定读取每块高度，重复测量结果一致 | 改用 Canvas measureText 自行排版，并同步修订第 10.3 节 |
| 文本行拆分 | Range 二分配合 getClientRects 能找到行边界；拆分点落于行边界；两段拼回还原原文 | 改为按整块分页并提示精简该段内容，同步修订第 10.3 节与 D13 的口径 |
| 多页串行渲染 | 按第 10.3 节逐页渲染可完成；单页 1080×2880 不触发移动端画布上限，峰值内存可接受 | 下调每页逻辑高度预算或输出倍率，并同步修订第 10.3 节参数 |
| 失败可捕获 | 渲染失败能被捕获并映射为可重试错误，不产生空白图冒充成功 | 补充超时与结果校验环节 |
| 保存行为 | 电脑可下载；手机与平板能打开图片并逐页保存；未支持时不误报成功 | 收敛为“图片已生成”提示，或另行评估系统分享能力 |

**边界**

- 验证在独立分支或临时页面进行，不并入主线提交；结论写入第 15 节，必要时更新第 2 节选型与第 10.3 节参数。
- 验证不改变第 10.2、10.3 节已确定的契约：CardViewModel 与 CardPage 是渲染层之上的接口，更换渲染器只替换第 10.3 节第 10 步的实现，不影响领域、预览与导出之间的数据流。先固定契约再验证渲染，正是为了让这一步的结论不会推翻前四步。
- 验证不接入真实 Key，也不依赖高德数据；使用手工构造的示例数据，并始终显著标注为示例（需求 11、需求 14.2）。

**真机验证与自动化测试的分工**

第 2 节列出的 Playwright 无法覆盖导出相关的关键行为：它的 WebKit 不等于 iOS Safari，图片保存与落盘判断（D14、T16）以及微信内嵌浏览器的保存路径都必须在真实设备上人工验证。这部分是可复现的人工测试成本，需在进入第 5 步之前完成一次并在第 8 步复测，不能用桌面模拟视口替代。

### 14.3 建议提交顺序

| 步骤 | 未来提交主题示例 | 本步内容 | 提交前门槛 |
| --- | --- | --- | --- |
| 0 | 不提交（临时验证） | 按第 14.2 节验证导出可行性，产出结论与最小样例 | 各验证项有明确结论；不通过项已确定处置方案并回写文档 |
| 1 | `chore: 建立项目基础与凭据隔离规则` | 初始化工程、语义样式基础、校验与测试工具、忽略规则、空环境示例 | 基础构建与检查通过，真实 Key 不在暂存区 |
| 2 | `feat(domain): 建立行程模型与可信计算口径` | 类型、字段校验、时间与连续步行、状态合成、基础测试 | 统一样例及未知/超限测试通过 |
| 3 | `feat(editor): 完成行程编辑与本地保存` | 节点编辑、顺序、跳过恢复、局部草稿及保存反馈 | 主流程与保存/取消失败路径通过 |
| 4 | `feat(ui): 完成手机平板电脑布局与大字预览` | 三端布局、组件、字体和无障碍交互 | 手机和平板横竖屏、电脑宽窄屏验证通过 |
| 5 | `feat(export): 实现大字卡分页与图片导出` | 固定逻辑宽度、分页、快照、保存反馈 | 三端图片可读、长内容完整、错误可恢复 |
| 6 | `feat(amap): 接入高德查询与步行路段` | 服务端凭据、检索、路线、错误与配额控制 | 契约测试及少量授权真实调用通过，前端无服务端 Key |
| 7 | `feat(planner): 实现约束规划与版本化重算` | 有限候选、锚点约束、Worker、候选差异及过期处理 | 排序、未知、预算和异步乱序测试通过 |
| 8 | `test: 补齐三端端到端验收与交付记录` | 完整旅程、性能与边界验证、必要说明同步 | 对应需求验收项逐项有结果，未支持项明确记录 |

- 步骤 0 不计入提交序列：它只产出结论，临时页面或分支在结论写入文档后即废弃，不进入主线，也不要求可构建；目的是让第 4、5 步建立在实测结论上，而不是建立在假设上。
- 每一步是一项独立主题，可进一步拆成“实现＋相应测试”的更小提交，但不把所有开发堆成最后一次大提交。
- 每个提交至少可构建且相关测试通过；后续阶段未交付时隐藏入口，不用空按钮冒充可用功能。
- 提交信息遵循英文 type(scope) 加中文说明，说明目的而不是只罗列文件。
- 本地提交与远端发布分离；推送、创建 PR 或部署需另行获得明确指令。

## 15. 设计决策与待核实事项

| 项目 | 当前设计结论 | 尚需核实 |
| --- | --- | --- |
| 产品命名 | 省脚力路线卡 / EasyWalk Route Card | 正式对外使用前的名称可用性 |
| 三端 | 同一业务代码，按视口宽度与输入能力适配 | 实际设备及浏览器版本的交付矩阵 |
| 高德 | Web 服务为数据层，暂不加载 JS 底图 | 提供的 Key 平台、接口授权、配额、服务端限制及许可 |
| 自动规划 | 小规模有界搜索，休息锚点固定 | 真实路段延迟及目标城市设施覆盖 |
| 大模型 | 不纳入 P0/P1 核心链路 | 是否需要自然语言入口由后续需求决定 |
| 数据保存 | 本地当前行程，无云同步 | 浏览器禁用存储、损坏数据及实际容量行为 |
| 导出 | 白底大字、固定逻辑宽度、逐页 PNG；html-to-image 与分块排版为待验证方案（第 2 节） | 渲染可行性与真实保存体验由第 14.2 节的步骤 0 在实现前验证；不通过时按该节处置改用 Canvas 渲染器，第 10.2、10.3 节契约不受影响 |
| 卡片契约与分页 | 预览与导出共用 CardViewModel，分页按第 10.2、10.3 节执行 | 真实排版下的测量精度、文本行拆分边界与最终页数，需用 6 景点加超长备注实测（A08、D13） |
| 纸面预览的窄屏降级 | 纸面固定 360px 逻辑宽度，可用宽度不足时等比缩小显示，工具栏不参与缩放（第 10.3 节） | 320px 缩放后的中文可读性、scale 占位高度修正与自动缩小提示的实际呈现，需真机实测（D01、T21） |
| 地图来源数据的可保存性 | 未经采纳的地图值不落盘；用户显式采纳后按用户估计保存，可在行中离线查看（第 8.5 节） | 高德许可对路段数值存留期限与复用的实际要求；核实通过后可按第 8.5 节简化路径省略采纳步骤 |
| 验收用例覆盖 | 第 13.2 节列出 22 条关键用例，并已标明每条所属文档 | A06、A09 与 D03、D04、D06、D12、D16 尚无对应用例，需补齐或明确由组件测试、浏览器测试、人工真机测试承担（其中 A06 与 D04 需在预览和导出两处都验证） |
| Git | 未来代码纳入版本管理、按步骤提交 | 进入实施时再检查仓库状态，本轮不执行 |

本说明书用于指导后续实现和验收，不替代实际接口授权核验、代码测试、浏览器测试或出行前人工确认。

## 16. 参考资料与证据边界

核对日期为 2026-09-22，均为公开说明；服务条款、接口能力和账号授权可能变化，实施前需再次核实。没有使用本次用户 Key 进行实测。

1. [高德地点搜索 v5 / POI 搜索 2.0](https://lbs.amap.com/api/webservice/guide/api-advanced/newpoisearch)：文本/周边接口、分页、business 扩展和检索数量限制。
2. [高德路径规划 v5](https://lbs.amap.com/api/webservice/guide/api/newroute)：步行接口、cost.duration、米/秒单位及阶梯路型。
3. [高德 Web 服务错误码](https://lbs.amap.com/api/webservice/guide/tools/info)：鉴权、配额、频率与其他业务错误分类。
4. [高德配额与流量说明](https://lbs.amap.com/api/webservice/guide/tools/flowlevel)：账号额度与控制台查询依据，不替代实际授权。
5. [申请 Web 服务 Key](https://lbs.amap.com/api/webservice/guide/create-project/get-key)：服务平台类型选择。
6. [高德 JS API 准备与安全配置](https://lbs.amap.com/api/javascript-api-v2/guide/abc/prepare)：JS API 平台 Key、安全码及推荐配置方式，与本次 REST 凭据不同。
7. [高德坐标系说明](https://lbs.amap.com/faq/advisory/others/39838)：GCJ-02 坐标边界。
8. [高德旧版路径规划](https://lbs.amap.com/api/webservice/guide/api/direction)：仅用于辨别旧版 100km 限制，不能直接套用为 v5 的已核实限制。

未完成项：用户 Key 的平台与有效性、账号实际配额、目标城市真实设施覆盖、数据存储和导出的具体许可，以及全部代码、设备和用户任务实测；本文没有将这些未完成项写成已验证事实。
