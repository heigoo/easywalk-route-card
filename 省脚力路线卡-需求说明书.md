# 省脚力路线卡｜产品需求与 UI / UE 设计说明书

- 文档版本：V1.4 草案（文档版本独立于产品发布版本）
- 编写日期：2026-09-22
- 更新日期：2026-09-22
- 本次更新：明确大字卡纸面宽度的窄屏降级规则（第 13.8 节），使 D01 与 D05 可同时满足；在来源记录中新增“用户采纳地图估算”（第 7.1 节），使行中可离线查看
- 产品中文名：省脚力路线卡
- 产品英文名：EasyWalk Route Card（设计暂定名）
- 产品形态：适配手机端、平板端、电脑端的响应式网页
- 地图供应商：高德地图；Web 服务 Key 类型、接口授权及配额待接入前核实
- 核心场景：双节带父母出游，提前了解步行负担，途中方便调整，生成大字版路线卡
- 文档状态：需求与设计建议，尚未实现；已指定高德，未验证凭据或实际设施数据覆盖
- 内容组成：第一至十二章为产品需求，第十三章为 UI 设计，第十四章为 UE 交互设计，第十五章为设计验收，第十六章为设计交付约定
- 配套文档：[省脚力路线卡详细设计](./省脚力路线卡-详细设计说明书.md)

本轮只交付文档与文本线框，不生成应用代码，不初始化、暂存或提交 Git；Git 管理与分步骤提交仅作为后续开发规范。用户提供的高德 Key 不在文档中复述，也不写入版本库或示例配置，当前未调用该 Key。

## 一、产品定位

### 1.1 一句话介绍

将选好的景点整理成一张看得懂、可调整、能保存的“省脚力”路线卡，让一家人知道走多久、在哪里歇、哪些地方累了可以不去。

命名规则：中文全称“省脚力路线卡”，中文简称“省脚力”，英文名“EasyWalk Route Card”，项目标识建议为 `easywalk-route-card`；界面以中文为主，英文作为品牌副标题，不代表首轮包含整站英文翻译。英文名尚未进行商标或域名可用性核验，不作独占使用承诺。

### 1.2 要解决的问题

1. 普通攻略强调景点推荐，缺少连续步行、休息位置和台阶等信息。
2. 景点之间距离看似不远，但园内游览、绕路和返程可能增加实际步行负担。
3. 父母不一定主动表达疲劳，行程缺少事先约定的休息安排与退出选项。
4. 路线信息散在地图、攻略和聊天记录中，长辈阅读和查找不方便。
5. 途中跳过景点后，原来的步行时间和路线可能已经不适用。

### 1.3 产品目标

- 行前：帮助安排者看清预计步行负担和待确认的信息。
- 行中：方便查看下一站、休息安排和可跳过景点。
- 分享：生成适合发到家庭群的大字图片，不要求接收者安装应用或注册。
- 自动规划增强：基于可靠数据和用户限制，辅助比较路线，而非凭空生成攻略。

### 1.4 非目标

首轮不做全功能旅游平台，不包含订票、酒店预订、餐饮推荐、实时拥堵预测、多人协同编辑和社交社区；不提供医疗运动处方，不承诺路线绝对安全、实时可用或全程无障碍。

## 二、目标用户与典型场景

| 用户 | 主要需求 | 主要操作 |
| --- | --- | --- |
| 行程安排者 | 少查几个页面，看清路线负担和备选安排 | 编辑景点、补充设施、调整路线、导出图片 |
| 同行父母及家人 | 字大、信息少而清楚，知道下一步怎么走 | 查看路线卡、了解休息安排与可跳过景点 |

典型流程：安排者选好几个景点，整理步行与休息信息，导出大字卡；途中有人走累了，安排者跳过一个可选景点，更新路段后重新导出。

## 三、版本范围与优先级

以下为建议分期，不代表用户已经确认开发排期；完整产品需求包括路线卡和自动规划两部分。

| 阶段 | 优先级 | 必须交付的能力 | 不包含的能力 |
| --- | --- | --- | --- |
| V1.0 路线卡闭环 | P0 | 手动编辑、时间汇总、休息与厕所备注、可选景点、大字预览、图片导出、本地保存 | 自动搜索设施、自动排序、云端分享 |
| V1.1 自动规划增强 | P1 | 地点搜索、地图步行数据、体力与时间约束、自动比较路线、跳站重算、来源标注 | 全国完整无障碍保证、实时座椅占用、实时排队预测 |
| V1.2 可选智能入口 | P2 | 自然语言转规划条件、基于已有结果生成易懂说明 | 让大模型自行编造设施、道路、开放时间或可行性结论 |

V1.0 可以作为独立工具使用，但不得把手工整理包装成自动规划；只有 V1.1 对应验收通过，才宣传自动规划能力。

## 四、页面与用户流程

### 4.1 页面结构

只设两个核心视图，避免为了展示功能拆分过多页面。

1. **行程编辑视图**：行程信息、景点顺序、路段耗时、设施备注、自动规划条件与结果。
2. **路线卡预览视图**：汇总信息、逐站安排、待确认事项、导出操作。

手机采用纵向编辑和独立预览；平板竖屏及分屏按可用宽度采用居中单列，横屏宽度足够时编辑与预览并排；电脑默认双列。布局按视口宽度判断，不按设备名称或 User-Agent 锁定，所有触屏尺寸均保留完整的点击操作。

### 4.2 手动整理流程

新建行程 → 添加起终点和景点 → 填写路段及景点信息 → 设置休息安排与可跳过景点 → 检查缺失信息 → 预览 → 导出图片。

### 4.3 自动规划流程

选择地点 → 设置必去/可选及时间、步行限制 → 获取地图数据 → 计算候选方案 → 展示结果与待核验项 → 用户确认应用 → 预览和导出。

自动生成的结果不直接覆盖用户当前方案，用户确认应用后才替换。

### 4.4 途中调整流程

选择“跳过此站” → 展示受影响路段 → 自动重算或要求手动更新 → 刷新汇总及限制检查 → 重新导出。

## 五、功能需求

### R01 行程创建与保存〔P0〕

- 支持填写行程名称、城市、出游日期、出发时间、起点及终点。
- 手动编辑允许先留空日期或时间；缺失时不展示精确到达时刻。
- 自动规划要求确认起终点；需要按开放时间和截止时间排程时，必须有出游日期及出发时间。
- 首轮支持 1～6 个景点，典型使用为 3～6 个；起终点和独立休息点不计入景点数量。
- 行程修改后自动保存到当前浏览器，刷新后可继续编辑。
- 本地保存不等于云端备份，需说明换设备或清理浏览器数据可能导致丢失。
- 删除整份行程前二次确认；保存失败时明确提示，不能显示“已保存”。

### R02 景点与路线编辑〔P0〕

每个景点支持：名称、预计停留时长、园内预计步行时长、必去/可选、休息安排、厕所及台阶等备注。

- 支持新增、删除、上移、下移；不把拖拽作为唯一排序方式。
- 新增景点默认保留在行程中，只有用户显式标为“可选”才允许自动省略。
- 景点停留时长包括园内步行和该景点内安排的休息，不重复加时。
- 园内步行时间允许未知，不能默认填写为零；已填写时不得超过景点停留时长。
- 每个相邻节点之间单独记录路段耗时及步行部分，手动版可以填写乘车接驳及其步行部分。
- 添加、删除或移动景点后，所有端点发生变化的路段数据失效，要求重新获取或填写。
- 景点地图坐标应尽量对应可通行的入口，不直接把景区中心点当作步行入口。

### R03 设施与提醒〔P0；自动检索为 P1〕

| 信息 | 记录内容 | 展示规则 |
| --- | --- | --- |
| 休息点 | 名称/位置、是否可坐、计划休息时长 | “附近有店”不等于有座位或可免费休息 |
| 厕所 | 名称/位置、开放信息、使用限制 | 未知开放状态必须标注，不能只显示“有厕所” |
| 台阶或坡道 | 所在路段、情况说明、替代通行信息 | 没查到台阶不能标成“无台阶” |
| 可选景点 | 可跳过标记及简短说明 | 显示“累了可跳过”，同时提示调整后需更新路线 |

- 支持用户手动补充和修改设施信息。
- 自动搜索所得设施首先是候选点，必须核对入口及到达路线，不能只按直线距离插入。
- 绕路寻找厕所或休息点所需步行必须纳入计算。
- 设施位置、开放情况、是否有座位分别记录可信状态，不能用一个“已确认”代替所有属性。

### R04 时间与步行负担汇总〔P0〕

路线卡至少显示：预计总步行时间、预计行程用时、最长连续步行时间或“待确认”、安排的休息次数。

**统一计算口径：**

- 预计总步行时间＝路段步行时间之和＋景点内部预计步行时间之和。
- 预计行程用时＝路段总耗时＋景点停留时长＋独立休息点停留时长＋已知等待时间。
- 绕行通过实际路段纳入，不重复额外累加；景点停留已包含的园内步行和休息不再次加入行程用时。
- 最长连续步行时间按两次满足用户休息条件的计划休息之间累计，短暂停留、拍照或站立参观不默认重置。
- 用户可设置一次有效休息的最低时长；乘车不自动视为有座位的有效休息。
- 景点内部是否休息、如何分段等信息不足时，最长连续步行时间标为待确认，不能给出假精确值。
- 所有时间均标“预计”；按同行者步速调整后的时间仍是估计，不是实际承诺。

缺失关键数据时展示“已知步行约 X 分钟，另有 N 项待补充”，不把部分合计标成完整总量；若停留时间或路段总耗时未知，不显示看似准确的结束时间。

### R05 大字路线卡〔P0〕

卡片内容顺序：

1. 行程名称、日期、适用版本或生成时间。
2. 预计步行与总用时摘要，以及是否有待确认事项。
3. 起点、逐站景点与终点。
4. 每段预计步行时间和其他交通方式。
5. 休息点、厕所、台阶等关键提醒。
6. “累了可跳过”的可选景点。
7. 数据来源摘要及“出发前核对开放与通行情况”提示。

展示要求：

- 首版采用线性路线示意，不冒充精确导航地图。
- 手机宽度 360px 正常查看时，主要路线文字建议不小于 22px，辅助文字不小于 18px。
- 正文文字与背景对比度不低于 4.5:1，不只通过颜色表达待确认、休息或可选状态。
- 重要操作触控区域不小于 44×44 CSS 像素，支持键盘操作及可见焦点。
- 长名称、多个备注和大量待确认项不得遮挡核心路线信息。
- 不使用“放心走”“绝对不累”等超出数据能力的文案。

### R06 图片导出〔P0〕

- 默认导出 PNG，大字图片可由用户自行发送到家庭群，系统不自动发送。
- 导出必须包含当前路线、必要提醒、待确认状态和生成时间，不能只导出有利信息。
- 导出前等待字体及必要资源加载完成，避免文字缺失或错位。
- 内容过长时支持分成多张连续卡片并标注页序，不为塞进一张图而无限缩小字体。
- 图片中的景点与路段不得在分页处被截断到无法理解。
- 导出失败提供可重试的明确提示，保留编辑内容，不生成空白图后提示成功。
- 如后续使用真实地图截图，必须遵守地图供应商对展示、署名及导出的许可要求。

### R07 途中跳站〔P0；自动重算为 P1〕

- “跳过”保留景点原始资料，区别于永久删除，便于恢复。
- 用户主动跳过必去景点时提示确认；自动规划不得自行丢弃必去景点。
- V1.0 跳站后，标出新形成路段待填写，相关完整合计和约束结论同步失效。
- V1.1 重新获取新路段数据，并重新检查步行、休息、开放时间和截止时间。
- 恢复景点同样触发受影响路段更新，不直接沿用可能过期的汇总结论。

## 六、自动规划详细需求〔P1〕

### 6.1 输入条件

| 输入项 | 规则 |
| --- | --- |
| 城市、起点、终点、景点 | 地图候选需要用户确认，处理同名景点和入口差异 |
| 必去/可选 | 必去不得省略；可选允许在满足限制的前提下取舍 |
| 出游日期、出发时间 | 用于开放时间、等待和结束时刻计算 |
| 最晚结束时间 | 可选；用户设置后按硬性限制检查 |
| 预计总步行上限 | 用户自行填写，不根据年龄推断医学意义上的安全值 |
| 连续步行上限、最低休息时长 | 用于检查休息安排，数据不全时不得声称满足 |
| 步行时间调整 | 可把地图基准时间调慢或手动修改，界面说明计算依据 |
| 台阶等通行要求 | 仅在数据可核实的路段判断；未知不等于满足 |

用户设定的上限不自动放宽；若没有设置某项上限，则不宣称已满足该项个人限制。

### 6.2 计算流程

1. 确认地点及可通行入口，统一地图供应商和坐标体系。
2. 获取候选节点之间的实际步行路线、距离和预计耗时，区分方向；查询失败与确实不可达分别处理。
3. 收集有来源的开放时间和候选休息点；无数据的属性保留未知。
4. 对有限候选景点的顺序和可选组合进行比较，必去全部保留。
5. 按实际到达时间检查能否在开放时段内完成游览，必要的开门等待纳入总用时。
6. 检查预计总步行、连续步行、休息与结束时间等限制。
7. 对满足可核验限制的方案排序，并输出计算结果、依据和无法核验的项目。

首轮自动规划以步行为主；确需乘车才能满足要求时给出接驳建议，由用户确认并补充交通信息，不宣称已经自动求出最优公共交通方案。

### 6.3 排序规则

在不违反已设置硬性限制的前提下，按以下顺序比较：

1. 保留全部必去景点。
2. 尽量保留更多用户已选择的可选景点。
3. 在保留景点数量相同时，优先预计总步行更少的方案。
4. 总步行相同时，优先最长连续步行更短、时间安排更合理的方案。

用户已经安排的独立休息点作为固定顺序的锚点，首轮只在相邻锚点之间重排所属景点，不自动删除休息点或跨锚点搬移景点；用户可先调整休息点再重新规划。该受限搜索范围必须明示，不宣称全局最优。

该顺序避免系统为了“最少走路”直接删掉所有景点，也避免只追求打卡数量而突破用户上限；首轮不引入难以解释的综合评分。

### 6.4 结果状态

| 状态 | 触发条件 | 用户可见结果 |
| --- | --- | --- |
| 计算完成 | 所需数据齐全，按估计值满足已设置限制 | 展示方案和估算依据，不表述为实际安全保证 |
| 待核验候选 | 存在开放时间、园内步行或通行设施等缺失信息 | 标出未知项及受影响限制，不计作已验证可行方案 |
| 未找到满足条件的方案 | 当前候选和已知数据下，所有比较方案均违反至少一项限制 | 说明主要冲突，建议用户调整条件，不自动放宽 |
| 数据获取失败 | 超时、配额不足或服务错误 | 保留原行程，允许重试或切回手动编辑，不使用直线距离伪装结果 |

“未找到方案”只针对当前候选、数据及搜索范围，不代表该城市不存在可行路线。

### 6.5 重算与请求成本

- 必去/可选、顺序、跳站、起终点、步速、休息安排或时间条件变化后，重新检查受影响结果。
- 日期变化后重新核验开放时间是否适用。
- 重用仍有效的路段结果，缓存策略遵守供应商许可和数据时效要求。
- 路线排列比较尽量在程序内完成，不能对每个排列重复请求相同路段。
- 计算过程可见；失败或用户未确认应用时，不破坏当前手动方案。

## 七、数据可信与安全边界

### 7.1 来源记录

路线和设施信息按事实项记录：值、来源类型、来源名称、获取时间、核对状态，必要时记录用户核对日期。

- 手动填写：标注“用户填写”，不自动升级为已核验。
- 地图返回：标注地图供应商与获取时间，地图存在该地点不代表此刻开放。
- 用户明确核对：记录核对日期和适用情况，不代表永久有效。
- 用户采纳地图估算：用户主动把地图返回的步行时间采纳为本行程估计后，该值按用户填写保存，可在本机离线查看，同时保留采纳时间与原始来源说明；采纳不等于重新核实地图数据，也不代表实时有效。未经采纳的地图值只在当前会话有效，不写入本机保存。
- 无来源或缺失：标注“待确认”，禁止由大模型补成确定事实。

### 7.2 不确定性处理

- 不知道有没有台阶，不能判定无台阶。
- 知道有厕所，不能推断全天开放或无障碍可用。
- 知道有咖啡店，不能推断有空座位或允许不消费休息。
- 不知道园内步行，不能把景点停留全部当成坐下休息。
- 地图预计时间不能替代老人实际体力判断。
- 导出图片是生成时快照，无法自动反映后续封路、闭馆或设施变化。

### 7.3 隐私与外部服务

- 默认不持续获取实时位置，用户手动选择起终点。
- V1.0 行程保存在本机浏览器，不默认上传到云端。
- 接入地图时说明所选位置及查询条件将发送给地图服务。
- 接入大模型必须由用户主动使用相关功能，并说明发送内容；不发送无关个人资料。
- 导出前提示用户检查酒店、家庭地址等不希望公开的信息。
- 用户备注按普通文本处理，避免作为可执行 HTML 渲染。

## 八、大模型是否必需〔P2〕

**不是必需依赖。自动规划由地图数据和可解释的约束计算完成。**

可选用途：

1. 将自然语言转换为结构化输入，例如“每走十五分钟想歇一歇”转成连续步行上限候选值。
2. 将程序计算的方案整理成简洁说明，例如为什么省略某个可选景点。
3. 帮助润色卡片标题和普通备注，不改变路线事实。

约束：

- 抽取后的条件需用户确认，不能悄悄修改限制。
- 模型输出必须通过字段及数值校验，不能直接写入已核验事实。
- 路线可行性由程序检查，不由模型一句“适合老人”判定。
- 模型不可用时，手动输入、规划计算和导出继续可用。

## 九、核心数据对象与实现建议

### 9.1 数据对象

| 对象 | 关键内容 |
| --- | --- |
| 行程 | 名称、城市、日期、起终点、当前版本、规划条件 |
| 景点/休息节点 | 唯一标识、名称、坐标、节点类型、停留与园内步行、必去/可选、跳过状态 |
| 路段 | 起止节点标识、交通方式、总耗时、步行耗时、距离、来源、有效状态 |
| 设施事实 | 关联节点或路段、属性值、来源、获取/核对时间、未知状态 |
| 规划结果 | 节点顺序、采用路段、时间汇总、约束检查、待核验项、结果状态 |

路段必须关联明确端点，不能仅按列表下标保存时间，否则重排或跳站会把旧时间套用到新路线。

### 9.2 建议技术方案

- 前端详细设计采用 React、TypeScript、Vite，三端复用同一业务实现；语义 CSS 变量和 CSS Modules 管理样式，基础弹层采用具备无障碍支持的组件。
- V1.0 存储：少量文本型行程采用浏览器本地存储；不提前建设账号和数据库。
- 图片导出：以 `html-to-image` 加分块排版为设计方案，重点验证中文字体、长内容和三端浏览器；正式实现以实测兼容性作为交付门槛。
- V1.1 服务端：采用 Node.js 与 Fastify 提供最小高德 Web 服务适配层，管理凭据、配额及合法缓存；前端不接收需要保密的服务端 Key。
- 当前线性路线卡无需加载高德 JS API 地图底图；若后续增加交互地图，单独确认 JS API Key、安全密钥与展示授权，不混用凭据。
- 规划逻辑：小规模候选集合采用枚举加约束剪枝，设施候选集合保持有限；先满足可解释性，不引入训练模型。
- V1.2：仅在自然语言入口确有价值时接入大模型，不把模型请求放进每次排序计算。

### 9.3 性能与使用质量

- 本地编辑与预览交互的目标响应时间不超过 300ms，以约定测试设备和典型 6 景点行程实测。
- 外部地图查询期间显示进度状态，避免重复提交；超时后可重试，不无限等待。
- 兼容手机常见浏览器与桌面 Chrome/Edge；如宣传微信内直接保存图片，需单独实测该场景。
- 不依赖悬停才能查看关键信息，不依赖登录才能阅读已导出的图片。
- 地图费用、配额、覆盖范围和使用条款应在选型时确认，不以“免费无限调用”为前提。

## 十、验收标准

### 10.1 V1.0 必须通过

| 编号 | 测试场景 | 通过标准 |
| --- | --- | --- |
| A01 | 新建并编辑 3 个景点 | 可完成编辑、预览、导出，所有必填缺失均有明确反馈 |
| A02 | 刷新页面 | 行程、顺序、备注、跳过状态和有效路段数据保留 |
| A03 | 添加、删除、重排节点 | 仅仍匹配端点的路段可沿用，变化路段标为待更新 |
| A04 | 跳过中间景点 | 不把原两段时间直接作为新路段时间，不展示旧完整合计 |
| A05 | 园内步行未知 | 显示已知步行合计及缺失项，不按零计算或假称符合上限 |
| A06 | 存在未知厕所开放/台阶信息 | 预览和导出均保留待确认状态 |
| A07 | 输入负数、非数值或园内步行大于停留 | 阻止保存该无效值并指出具体字段 |
| A08 | 长景点名称、中文备注、6 个景点及多个休息点 | 大字卡清晰，无文字裁切；分页内容连续 |
| A09 | 模拟保存或导出失败 | 不假报成功，编辑内容保留，提示可理解 |
| A10 | 删除行程 | 需确认；取消删除不损失内容 |

### 10.2 V1.1 必须通过

| 编号 | 测试场景 | 通过标准 |
| --- | --- | --- |
| B01 | 多个必去与可选景点 | 必去均保留，自动取舍仅作用于可选景点 |
| B02 | 全部已核验候选均超过用户上限 | 不自动放宽条件，显示冲突和可调整建议 |
| B03 | 设施或时间数据不足 | 输出待核验候选，不标成已满足全部要求 |
| B04 | 跳站后的直达路段与原路段不同 | 查询并使用新路段，重算总量、连续步行及时间窗口 |
| B05 | 地图超时、配额不足、地点不可达 | 区分失败原因，保留原行程，不以模型或直线估算冒充地图结果 |
| B06 | 开放时间、最晚结束时间、休息安排冲突 | 识别具体不满足项，等待和休息正确纳入计算 |
| B07 | 同名地点、错误入口或混合坐标体系 | 用户确认正确地点，坐标统一后再计算，不静默定位到异地 |
| B08 | 重复比较相同路段的排列 | 不因排列数量成倍重复请求地图，配额使用可追踪 |

### 10.3 统一计算测试样例

假设 B 为终点，各段数据及园内步行均已明确；A、B 内非步行游览均为站立参观，不另设满足条件的休息。

- 起点 → A：步行 12 分钟。
- A 停留：30 分钟，其中园内步行 8 分钟。
- A → 独立休息点：步行 8 分钟。
- 独立休息点：坐下休息 10 分钟，满足用户设置的最低休息时长。
- 独立休息点 → B：步行 10 分钟。
- B 停留：30 分钟，其中园内步行 5 分钟。

预期结果：

- 预计总步行＝12＋8＋8＋10＋5＝43 分钟。
- 预计总用时＝12＋30＋8＋10＋10＋30＝100 分钟。
- 两个连续步行累计区间分别为 28 分钟、15 分钟，最大值为 28 分钟。
- 若用户设置连续步行上限为 20 分钟，该方案不得判为满足要求。
- 若删除 A 的园内步行数据，应显示“已知步行约 35 分钟，另有 1 项待补充”，连续步行结论变为待确认；不得显示总步行 35 分钟。

## 十一、演示与交付要求

建议选一座城市的一条可核实路线演示，不以未经验证的全国设施覆盖作为展示卖点。

演示顺序：

1. 输入包含必去、可选及一个休息点的行程。
2. 展示预计步行、休息安排和待确认项。
3. 运行自动规划（交付 V1.1 时），说明取舍依据。
4. 跳过一个景点，展示受影响路段与时间确实更新。
5. 导出大字卡，在手机实际打开检查可读性。

验收交付包含可运行网页、注明来源与日期的示例数据、对应阶段测试结果；示例或虚构数据必须显著标注，不冒充真实线路验证结果。

## 十二、实施前待确认项

| 项目 | 建议默认值或已明确要求 | 需要确认的原因 |
| --- | --- | --- |
| 首次交付范围 | 先完成 V1.0，再交付 V1.1；当前仅详细设计 | 路线卡能独立使用，自动规划增加外部数据和验证成本 |
| 高德接入条件 | 供应商已明确为高德，使用服务端 Web 服务接入；地图来源的路段时间经用户采纳后按用户估计保存（第 7.1 节） | 用户提供的 Key 类型、接口授权、配额与部署环境限制，以及路段数值的存留期限与复用许可尚未核实 |
| 自动规划首轮展示范围 | 一座城市、少量已核实景点及休息点 | 便于验证正确性，避免全国数据质量无法保证 |
| 产品英文名 | EasyWalk Route Card，设计暂定名 | 未核查商标、域名或应用市场重名情况 |
| 三端支持 | 手机、平板、电脑均需支持 | 浏览器版本与实际设备范围在实施验收前固定 |
| 大模型 | 首轮不接入 | 非规划核心，减少幻觉、延迟和调用成本 |
| 云端分享与账号 | 首轮不建设，以导出图片为主 | 降低使用门槛和隐私、存储、权限管理成本 |
| Git 管理 | 后续代码需 Git 管理并分步骤提交 | 仅列为开发规范，本轮不执行任何 Git 写操作 |

最终交付原则：先让一条路线的信息可信、调整正确、图片看得清，再扩展城市和智能能力。

## 十三、UI 视觉与界面设计说明书

### 13.1 设计目标与适用范围

UI 设计服务于两种不同任务，而不是把同一套复杂编辑界面交给所有人。

| 场景 | 设计重点 | 默认外观 |
| --- | --- | --- |
| 安排者编辑行程 | 信息有层次、可快速修改、状态明确 | 中性黑灰工作区，绿色强调主要操作 |
| 父母查看路线卡 | 字大、户外可读、逐站清楚、提醒不遗漏 | 白底深色文字，少量绿色与警示色 |

- 黑灰编辑区参考 Qoder 的中性色调，不使用偏蓝的深色背景。
- 白底路线卡是独立的阅读与导出样式，不增加全站主题切换功能。
- 视觉参考纸质行程单和清楚的路线时序图，不照搬地图导航或聊天机器人界面。
- 不用大幅旅游照片、节庆动画、背景纹理或装饰渐变占据首屏；节日感仅通过行程名称和短文案体现。
- 全界面优先突出三件事：接下来去哪里、预计走多久、哪里可以休息。

### 13.2 信息架构与视图清单

延续第四章的两个核心视图，其他界面均为编辑视图内的局部面板或对话框，不额外建设首页、个人中心或社区。

| 编号 | 界面 | 主要信息与操作 | 阶段 |
| --- | --- | --- | --- |
| UI-01 | 空行程状态 | 产品名称、简短说明、行程基础信息、添加第一个景点 | P0 |
| UI-02 | 行程编辑 | 基础信息、景点序列、路段、设施备注、时间汇总、预览入口 | P0 |
| UI-03 | 节点/设施编辑面板 | 当前对象、字段、未知状态、来源、确认或取消 | P0 |
| UI-04 | 大字卡预览 | 白底路线卡、重要提醒、返回编辑、图片导出 | P0 |
| UI-05 | 规划条件与结果面板 | 约束输入、查询状态、候选结果、变化说明、应用方案 | P1 |
| UI-06 | 删除/跳过必去确认框 | 明确对象与影响，确认或取消 | P0 |
| UI-07 | 导出反馈区域 | 正在生成、生成完成、失败重试、保存说明 | P0 |

P1、P2 未交付时隐藏对应入口，不摆放无法使用的“智能规划”按钮制造已有功能的错觉。

### 13.3 桌面布局线框

适用宽度不小于 1024px：工作区最大宽度 1200px 居中，左右留白不小于 24px；编辑列自适应，预览列基准宽度 360px，列间距 24px。

下列数字引用第 10.3 节的计算样例，地点均为示例名称，不表示真实景点或已验证的出游路线。

```text
省脚力路线卡                           已保存在本机      [导出图片]
少赶一站，多歇一会

行程编辑                               大字路线卡预览
┌────────────────────────┐            ┌────────────────────┐
│ 行程名称 / 城市 / 日期   │            │ 示例行程            │
│ 起点 / 终点 / 出发时间   │            │ 非真实地点          │
├────────────────────────┤            │ 预计步行 43 分钟    │
│ 起点                    │            │ 行程用时 100 分钟   │
│   步行约 12 分钟 [编辑] │            │ 连续最多 28 分钟    │
│ 1 景点 A        [必去]  │            │                    │
│   停留30分 · 园内走8分  │            │ 起点 → 景点 A      │
│   [编辑] [上移] [下移]  │            │ 步行约12分钟        │
│   步行约 8 分钟         │            │                    │
│ 休息点 · 坐下歇10分钟   │            │ 歇一歇 · 10分钟     │
│   步行约 10 分钟        │            │                    │
│ 2 景点 B        [可选]  │            │ 景点 B · 累了可跳过 │
│   [编辑] [跳过]         │            │                    │
│ [添加景点] [添加休息点] │            │ 来源与必要提醒      │
└────────────────────────┘            └────────────────────┘
[规划条件与自动规划，仅 P1 提供]
```

- 预览区域只承担阅读反馈，不把编辑按钮带入白色卡片。
- 不用复杂地图抢占主操作空间；路线用有明确顺序的纵向节点表达。
- 页面正常纵向滚动，首轮不强制左右两列独立滚动，避免嵌套滚动条。
- 长预览按顺序显示，不为了固定首屏高度缩小文字；超宽屏保持最大内容宽度，不无限拉伸表单。

### 13.4 手机布局线框

手机编辑和大字预览分别显示，切换时保留草稿和原滚动位置；预览中的浏览器返回操作应回到编辑视图，而不是直接离开工具。

```text
编辑视图                         大字预览视图
┌──────────────────────┐        ┌──────────────────────┐
│ 省脚力路线卡          │        │ [返回编辑] 大字预览  │
│ 已保存在本机          │        ├──────────────────────┤
├──────────────────────┤        │ 示例行程              │
│ 行程信息        [展开]│        │ 非真实地点            │
│ 预计步行43分          │        │                      │
│ 连续最多28分          │        │ 预计步行 43 分钟     │
├──────────────────────┤        │ 全程约 100 分钟      │
│ 起点                  │        │ 连续最多 28 分钟     │
│ 步行约12分钟 [编辑]   │        │                      │
│ 1 景点 A    [必去]    │        │ 起点                  │
│ 停留30分 / 园内走8分  │        │   步行约12分钟       │
│ [编辑] [更多操作]     │        │ 1 景点 A             │
│ 步行约8分钟           │        │   步行约8分钟        │
│ 休息10分钟            │        │ 坐下歇10分钟         │
│ 步行约10分钟          │        │   步行约10分钟       │
│ 2 景点 B    [可选]    │        │ 2 景点 B             │
│ [编辑] [跳过]         │        │ 累了可跳过            │
│ [添加景点]            │        │ 来源、生成时间及提醒  │
│ [添加休息点]          │        │                      │
├──────────────────────┤        ├──────────────────────┤
│ [预览路线卡]          │        │ [导出图片]           │
└──────────────────────┘        └──────────────────────┘
```

- 顶部只保留标题和保存状态，避免在 320px 宽度并排塞入多个操作。
- 底部操作栏独立于导出内容，预留自身高度和设备安全区，不遮住末尾表单或提醒。
- 系统键盘弹起时，优先保证输入框、错误说明和确认按钮可见，必要时取消操作栏吸底。
- 已填写的基础信息可以折叠；有错误时自动展开并定位，不能把错误藏在折叠区。
- 必要提醒和超限提示不默认折叠；可折叠的是来源详情和次要编辑信息。

### 13.5 视觉令牌

以下为首版设计基线。实现时统一定义语义令牌并以 HSL 存储，组件引用令牌，不散写任意颜色；HEX 仅供设计核对。

| 令牌 | HEX | HSL | 用途 |
| --- | --- | --- | --- |
| `app-bg` | `#0d0d0d` | `0 0% 5.10%` | 编辑工作区背景 |
| `surface` | `#151515` | `0 0% 8.24%` | 表单和面板 |
| `surface-raised` | `#1d1d1d` | `0 0% 11.37%` | 输入区、浮层 |
| `surface-hover` | `#242424` | `0 0% 14.12%` | 中性控件悬停背景 |
| `divider` | `#262626` | `0 0% 14.90%` | 装饰分隔线，不承担识别输入框的职责 |
| `control-border` | `#707070` | `0 0% 43.92%` | 需要可辨识边界的输入控件 |
| `text-primary` | `#f5f5f5` | `0 0% 96.08%` | 编辑区主要文字 |
| `text-secondary` | `#b8b8b8` | `0 0% 72.16%` | 编辑区次要文字 |
| `text-muted` | `#9c9c9c` | `0 0% 61.18%` | 辅助说明，不用于隐藏重要错误 |
| `primary` | `#2f9e44` | `131.35 54.15% 40.20%` | 主要操作、深色区焦点环 |
| `primary-hover` | `#3cb853` | `131.13 50.82% 47.84%` | 主要操作悬停 |
| `on-primary` | `#0d0d0d` | `0 0% 5.10%` | 绿色按钮上的深色文字 |
| `paper-bg` | `#ffffff` | `0 0% 100%` | 阅读卡与导出底色 |
| `paper-text` | `#171717` | `0 0% 9.02%` | 大字卡主文字 |
| `paper-secondary` | `#525252` | `0 0% 32.16%` | 大字卡辅助说明 |
| `paper-accent` | `#166534` | `142.78 64.23% 24.12%` | 白底卡强调文字及焦点环 |
| `danger` | `#b91c1c` | `0 73.71% 41.76%` | 确认删除等破坏性操作 |
| `on-danger` | `#ffffff` | `0 0% 100%` | 破坏性操作上的文字 |

状态颜色也必须做成语义令牌；以下每组均按“文字 / 背景”定义，实现时同样转成 HSL。

| 状态 | 深色编辑区 | 白底路线卡 | 语义限制 |
| --- | --- | --- | --- |
| 完成/可用 | `#65c777 / #17271b` | `#166534 / #eaf5ee` | 表示明确操作结果，不表示医学安全或全程无障碍 |
| 待确认/超限提醒 | `#f0bd62 / #2b2317` | `#854d0e / #fff4d6` | 必须写出“待确认”或“超过上限”，不能只变色 |
| 错误/失败 | `#ff9b9b / #321d1d` | `#991b1b / #fee2e2` | 用于无效输入、保存或数据请求失败 |
| 中性信息 | 继承次要文字及面板色 | 继承辅助文字及纸面色 | 用户填写、示例、来源说明 |

已按 sRGB 相对亮度公式核算的指定配色：绿色主按钮深色文字约 5.64:1，白底正文约 17.93:1，白底辅助文字约 7.81:1，白底警示文字约 6.25:1；输入边界对抬升面板约 3.40:1。上述核算不替代实现后的透明度、禁用态、叠层及焦点样式实测。

绿色主按钮不直接配普通字号白字；重要控件也不能只靠低对比度的装饰分隔线识别。

### 13.6 字体、间距与尺寸

| 类别 | 规格 |
| --- | --- |
| 字体 | 优先系统中文无衬线字体，如苹方、微软雅黑及系统 sans-serif；不依赖外部字体才能导出 |
| 编辑区层级 | 页面标题 28px / 600，区块标题 20px / 600，正文及输入 16px / 400，辅助说明 14px / 400 |
| 路线卡层级 | 核心数字 32px / 600，行程标题 28px / 600，节点及主要路线 22px / 600，辅助信息 18px / 400 |
| 行高 | 标题约 1.3，正文与提醒约 1.5；所有容器允许随换行增高 |
| 间距刻度 | 4、8、12、16、24、32、48、64px，按语义层级选用 |
| 编辑卡片 | 内边距 16px，区块间距 24px，相关字段间距 12～16px |
| 阅读卡片 | 基准宽度 360px，内边距 16px，节点之间至少 24px |
| 控件 | 默认高度 48px，触控区域最小 44×44px；长按钮文字允许增高，不裁切 |
| 圆角 | 输入及按钮 10px，内容卡 16px，抽屉或对话框 20px |
| 阴影 | 常规卡片主要依靠层次和间距区分，只在浮层使用轻量阴影 |
| 焦点 | 2px 焦点环并外扩 2px，颜色按当前深色/白底上下文选择 |

时间数值和单位保持相邻，金额或体力分数不作为装饰性指标；不使用“老人模式评分”“安全指数”等没有依据的评级。

### 13.7 组件与状态规范

| 组件 | 主要表现 | 必须提供的状态或替代操作 |
| --- | --- | --- |
| 主按钮 | 绿色实底、深色文字；同一操作区域只有一个主要行动 | 默认、悬停、焦点、按下、处理中、不可用 |
| 次按钮 | 中性表面及可辨识边界 | 焦点可见，不与主按钮争抢注意力 |
| 输入框 | 外置字段名称、明确单位、足够边界对比度 | 未填写、有效、无效、只读；占位文字不代替标签 |
| 时间输入 | 数字输入＋分钟单位，可标“未知” | 空白不自动改成 0，错误就地解释 |
| 必去/可选选择 | 文本单选组或分段控件 | 明确选中态，支持键盘切换 |
| 景点节点卡 | 序号、名称、停留/园内步行、提醒、操作 | 必去、可选、已跳过；跳过与删除分开 |
| 路段连接行 | “步行约 X 分钟”等文字连接相邻节点 | 已填写、待更新、获取中、失败 |
| 设施标签 | 图标加文字，如“厕所：开放时间待确认” | 不只显示厕所或座椅图标，不默认暗示可用 |
| 汇总条 | 步行、用时、连续步行、休息 | 完整估计、已知部分、超限、待确认 |
| 提醒条 | 简述问题＋查看或处理入口 | 持续显示直到处理，不能只用短暂 Toast |
| 局部面板 | 展示一个节点、设施或规划结果 | 标题、关闭、确认、取消，焦点可返回来源控件 |

图标采用同一套线性图标，常用为 20～24px；重要操作必须有文字或可访问名称，不以表情符号作为核心操作标识。

### 13.8 响应式规则

| 视口宽度 | 常见覆盖场景 | 布局 | 交互适配 |
| --- | --- | --- | --- |
| 320～639px | 手机竖屏、平板窄分屏 | 单列，左右间距 16px，编辑/预览分开 | 底部主要操作；复杂表单使用全屏面板 |
| 640～1023px | 平板竖屏、手机横屏、电脑窄窗口 | 单列内容居中，编辑区最大宽度 720px | 可并排放两个短字段，长字段仍独占一行 |
| 不小于 1024px | 电脑、平板宽横屏 | 最大宽度 1200px，编辑与 360px 预览并排 | 节点编辑可使用约 480px 的侧面板 |

- 手机、平板、电脑使用同一行程模型与功能集合，不按设备名称屏蔽编辑、规划或导出。
- 平板必须验证横竖屏切换、分屏、触摸与外接键盘；宽屏仍保持至少 44px 的触控目标，不把操作藏在悬停中。
- 使用动态可视高度与安全区处理浏览器工具栏、软键盘和底部操作栏，不能只按固定屏幕高度布局。
- 断点切换不丢失输入、展开状态或选中的节点。
- 页面、模态框、状态提示和长地点名称都不得产生横向滚动。
- 窄屏优先换行而非减小字体，长地点名不靠省略号隐藏唯一可识别的信息。
- 大字卡的纸面宽度属于排版规格，不参与上述重排：预览可用宽度小于纸面基准宽度时，纸面整体等比缩小显示，字号、换行与分页均不改变；此时纸面之外（工具栏等）仍保持左右间距。360px 视口下纸面按基准宽度 1:1 显示，320px 视口下缩小显示且不产生横向滚动。导出始终按基准宽度输出，不受视口影响。
- 简短选择可用底部面板，长表单改用全屏，避免大量字段挤在过矮弹层内。
- 200% 缩放和中文长文本应保持可读；一旦宽度不足，按窄屏布局重排。

### 13.9 大字卡导出视觉规范

- 以逻辑宽度 360 CSS 像素作为导出排版基准，默认输出宽度 1080 像素，即按 3 倍像素密度渲染。
- 字号先按 22px / 18px 等逻辑尺寸排版，再整体渲染；不能在 1080 像素画布上直接使用 22 像素正文。
- 同一行程的多页图片保持相同宽度、字号、边距和状态色，标明“第 X / Y 张”。
- 按完整节点和相邻路段合理分页；续页需保留承接的上一站名称，避免不知道这一段从哪里出发。
- 单条备注过长时标注“续”，保留完整内容，不靠截断、溢出或缩小字体处理。
- 被跳过的节点不再出现在实际路线中；仍参与行程的可选节点保留“累了可跳过”标签。
- 不能把标题、绿色装饰或二维码放大到挤压路线内容；首轮不增加没有在线目标的二维码。
- 黑灰编辑界面、表单、按钮、浮层、焦点环均不进入导出图片。
- 关键信息缺失或方案超限时，导出卡必须显著标“草稿”及具体提醒，不能通过截图式导出隐藏问题。

## 十四、UE 用户体验与交互设计说明书

### 14.1 体验原则

1. **先能做成，再逐步完善**：允许建立不完整草稿，不要求第一次输入就填完所有设施信息。
2. **未知不等于没有**：用明确文字表达未知，不通过默认值和图标替用户作判断。
3. **编辑者与阅读者分开**：复杂操作留给安排者，大字卡只呈现路线和必要提醒。
4. **调整后让影响可见**：跳站、改顺序、改时间后，明确哪些数据需要重算。
5. **有反馈，也有退路**：计算失败、保存失败或找不到方案时保留原内容，提供下一步操作。

### 14.2 首次使用与渐进填写

- 首次进入直接显示空行程编辑界面，不先进入营销首页或注册页。
- 引导文案建议为“先放上想去的地方，再看看一路要走多久”，主要操作为“添加第一个景点”。
- 添加景点先收集名称、必去/可选及停留信息，园内步行、设施、来源等按分组展开。
- 字段是否必填随操作目的变化：保存草稿不强迫补齐全部字段，运行自动规划才校验必要地点和条件。
- 默认空白行程不填入看似真实的设施和时间；演示内容必须由用户主动打开，始终标注为示例。
- 保存状态只表示存储结果，“已保存在本机”不能让用户误以为路线已核验。

### 14.3 编辑、确认与取消

- 主编辑区的有效修改自动保存，不设含义不清的全局“保存并完成”。
- 节点、设施等弹层内采用局部草稿，点击“确认修改”后提交；点击取消不改变原数据。
- 有未确认修改时关闭弹层，明确询问“放弃本次修改”或“继续编辑”。
- 删除整份行程、删除节点等丢失内容的操作需明确对象；整份删除必须二次确认。
- 已跳过节点在编辑区归入“已跳过”区域，保留恢复入口；不与当前有效路线混在一起。
- 重新排序后，状态提示说明“位置已调整，相关路段待更新”，而不是只显示“操作成功”。

### 14.4 地点搜索与设施填写

- P0 手动填写地点名称不暗示已匹配地图；P1 搜索结果必须显示城市、区县及入口信息供用户选择。
- 仅输入文字但未确认地图候选时，不自动采用列表第一项。
- 没有搜索结果时允许返回手动整理；如缺少坐标影响规划，明确说明暂不能自动计算该路段。
- 厕所开放、是否可坐、有无台阶等属性提供“待确认”选项，初始未知不表现为未勾选的“无”。
- 用户点击“已核对”时针对具体属性记录核对日期，不提供一键把全部设施升级为已核验的操作。
- 占位文案仅演示输入格式，不预设数值；错误字段保留原输入，不静默修正负数或不合理时长。

### 14.5 自动规划交互〔P1〕

1. 用户进入“规划条件”，填写或核对个人上限；未设置的条件明确显示“未限制”，不预设通用老人体力值。
2. 点击“计算路线”后，显示“正在获取步行数据”“正在比较路线”等与实际进度一致的阶段，不伪造百分比。
3. 当前请求期间防止重复提交，仍允许返回编辑；编辑后，基于旧条件的结果标为过期，不允许直接应用。
4. 最新有效结果展示保留/省略的景点、预计步行、最长连续步行、结束时间和待核验项；数据未知时使用对应占位说明而非精确数字。
5. “应用此方案”只作用于用户正在查看的结果；应用前列出与当前行程不同的顺序、可选景点及休息安排。
6. 待核验候选允许“作为草稿应用”，但持续保留警示；已知违反硬性限制的方案不作为满足条件的推荐。
7. 关闭结果面板或请求失败不改变原行程；迟到的旧请求不能覆盖新输入或新结果。

未找到满足条件的方案时，建议区分“少去一个可选景点”“调整结束时间”“补充接驳”几个可理解的动作，由用户选择，不代替用户提高步行上限。

### 14.6 跳站与恢复交互

- 可选景点的“跳过”直接可见；必去景点需要说明影响并确认，不能误触一次就丢失。
- 跳过后原节点资料仍保留，新相邻路段立即进入“待更新”或“重新计算中”。
- 重算期间不得继续把旧总步行标成当前路线的最新合计。
- P0 提示“已跳过景点 A，请补充起点到下一站的时间”，可直接进入该路段编辑。
- P1 成功后展示变化摘要，例如“已跳过 1 站，更新 2 段路线”；只有两版数据完整且可比时才显示“预计少走 X 分钟”。
- P1 失败后提供“重试”“手动填写”和恢复被跳过景点的入口，不让用户被困在无法导出的空白状态。
- 恢复节点遵循同样的数据失效与重新校验规则。

### 14.7 状态反馈与文案

将“存储/请求状态”和“路线可信状态”分开呈现，避免“保存成功”或“计算结束”掩盖路线问题。

| 情况 | 建议文案 | 下一步动作 |
| --- | --- | --- |
| 首次空状态 | 先添加想去的地方 | 添加第一个景点 |
| 本地保存完成 | 已保存在本机 | 无需打断操作 |
| 本地保存失败 | 暂时无法保存，当前编辑内容仍保留 | 重试；允许预览并导出可用草稿 |
| 数据部分缺失 | 已知步行约 35 分钟，另有 1 项待补充 | 定位缺失字段 |
| 设施属性未知 | 厕所位置已记录，开放时间待确认 | 查看来源或补充核对 |
| 已知超限 | 最长连续步行约 28 分钟，超过你设置的 20 分钟 | 调整休息或减少景点 |
| 未找到满足方案 | 当前候选中未找到符合这些条件的路线 | 查看冲突，修改条件 |
| 地图请求失败 | 步行数据暂时获取失败，原行程未改变 | 重试或手动编辑 |
| 跳站重算 | 正在更新新路段，完整步行合计暂不可用 | 等待或手动更新 |
| 结果已过期 | 行程已修改，请按新条件重新计算 | 重新计算 |
| 导出生成中 | 正在生成大字卡 | 防止重复触发 |
| 图片生成完成 | 图片已生成，请保存后发到家庭群 | 下载或打开图片查看 |

- 不能确认浏览器已经保存文件到设备时，不提示“已保存到相册”。
- 已知违反上限时优先提示超限，同时保留未知项；不能因有部分数据缺失就隐去已经确定的冲突。
- 错误及重要提醒持续保留，并通过文字、图标和状态色共同表达。
- 保存完成等非关键反馈可轻量展示，但不能频繁播报或弹出 Toast 打断输入。

### 14.8 预览与导出的决策规则

| 当前内容 | 是否允许预览 | 导出行为 |
| --- | --- | --- |
| 没有有效景点 | 显示空预览，不伪造路线 | 不可导出，旁边解释原因并提供添加入口 |
| 存在负数、非数值或互相矛盾的无效输入 | 标出错误，不展示误导性合计 | 阻止导出，定位首个错误字段 |
| 有未填时长或待确认设施 | 可预览草稿 | 按钮显示“导出草稿”，图片保留缺失项和警示 |
| 已知不满足用户上限 | 可查看当前草稿及冲突 | 只允许导出带超限说明的草稿，不包装为推荐方案 |
| 数据完整且无已知冲突 | 正常预览 | 导出图片，保留“预计”及必要来源说明 |
| 计算结果尚未应用 | 预览当前已确认行程 | 不把未应用候选混入当前导出 |

导出前在预览区提示检查酒店或家庭地址等信息，不额外收集联系人，也不自动打开群聊发送。

多页导出必须向用户展示页数和逐页保存入口，不能假设所有手机浏览器都允许批量自动下载；如需要系统分享能力，须按支持情况验证后再提供。

### 14.9 焦点、键盘与动效

- 控件顺序与视觉阅读顺序一致，点击字段标签可聚焦对应输入。
- 打开对话框后焦点进入对话框，不能移到遮罩后的页面；关闭后返回触发控件。
- ESC 触发关闭意图，但存在未确认修改时仍走放弃确认，不直接丢弃输入。
- 表单错误与字段建立可访问关联，关键状态变化提供适度的读屏通知，不逐字播报输入和保存动作。
- 不能只靠拖拽排序、滑动删除、长按或悬停查看内容，均提供明确替代控件。
- 状态切换建议 120～180ms，面板进出不超过 200ms；遵循减少动态效果的系统设置。
- 不自动滚动到下一站、不闪烁催促、不播放声音；发生错误定位时才作必要的滚动和焦点引导。

## 十五、UI / UE 设计验收清单

以下为新增的设计验收要求，不代表已经执行；产品功能仍需通过第十章验收。

| 编号 | 验收场景 | 通过标准 |
| --- | --- | --- |
| D01 | 320、360、390px 手机宽度 | 无横向滚动；操作可触达；长名称及提醒可完整阅读 |
| D02 | 768、1024、1440px 及 3840px 宽屏 | 断点切换合理；预览不挤压表单；超宽屏不无限拉伸内容 |
| D03 | 200% 缩放、系统大字体、软键盘弹起 | 无内容重叠或输入遮挡，底部栏不覆盖确认操作 |
| D04 | 两种视觉上下文的文字和状态 | 正文对比度至少 4.5:1，重要控件及焦点满足相应可辨识要求 |
| D05 | 白底大字卡在 360px 宽度查看 | 主路线不少于 22px、辅助信息不少于 18px；无须先放大才能认站名和提醒 |
| D06 | 正常编辑到预览再返回 | 草稿、节点状态、滚动位置合理恢复，无误离开或输入丢失 |
| D07 | 未填写、填写 0、无效数值 | 三者能够区分，未知不变成零，无效值不能进入正常导出 |
| D08 | 未核验设施和已知超限同时出现 | 两类问题都可见，不被绿色完成态或保存成功覆盖 |
| D09 | 计算中修改行程、旧请求迟到 | 旧结果不能应用或覆盖当前内容，重新计算入口明确 |
| D10 | 跳过/恢复节点与重算失败 | 新路段失效可见，旧合计不冒充新结果，用户可重试或手动继续 |
| D11 | 面板取消、关闭及删除确认 | 取消不改原数据；关闭不丢未确认修改；删除明确对象 |
| D12 | 仅键盘和读屏操作 | 可完成添加、编辑、排序、预览及导出；焦点进出正常 |
| D13 | 6 个景点、多个休息点及超长备注导出 | 字号不缩小，分页连续，提醒、来源及页码不丢失 |
| D14 | 手机图片生成与保存 | 生成和落盘反馈区分，逐页保存可用；未支持场景不承诺成功 |
| D15 | P0 版本查看全部操作 | 不出现不可用的 P1/P2 假入口，无自动生成的伪造设施 |
| D16 | 减少动态效果及失败状态 | 动效可减少，错误持久可理解，操作不依赖瞬时提示 |
| D17 | 平板横竖屏、窄分屏、触控与外接键盘 | 功能完整；切换不丢草稿、焦点和展开状态；导出采用固定逻辑宽度，不随设备字号失真 |

建议分别让安排者完成一次“编辑—跳站—导出”，让长辈或目标阅读者完成一次“找下一站—找休息安排—识别待确认事项”的任务测试，记录观察结果；不能只用开发者截图评价实际可用性。

## 十六、设计交付与后续实现约定

- 本次设计交付包含：页面信息架构、桌面/手机文本线框、视觉令牌、组件规格、交互流程、状态文案和设计验收清单。
- 文本线框用于约定信息顺序与操作位置，不作为像素级最终效果图；后续如制作高保真图或交互原型，应沿用相同范围和令牌。
- 实现时为 UI-01 至 UI-07 留存对应阶段的界面截图；未实现阶段不伪造截图或可用状态。
- 后续样式、组件与导出卡应共享对应的语义令牌，编辑区与纸面区通过明确的样式上下文隔离。
- UI 修改若影响路线字段、可信状态或计算口径，需同步检查第五、六、七章，不仅修改视觉文案。
- 页面开发完成后再进行浏览器和实际图片阅读验证；当前说明书不替代可运行原型及用户验收。
